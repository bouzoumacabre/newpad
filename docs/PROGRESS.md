# Newpad — Inventaire, phases, état d'avancement

Dernière mise à jour : 25 août 2026 (4ème passe d'audit : **fuite monétaire réelle** trouvée par vérification de l'invariant de conservation sur les données de production — voir §5duodecies ; passes précédentes du même jour — voir §5undecies, §5decies, §5nonies).

## 5duodecies. Conservation de la monnaie : fuite trouvée sur les données réelles (25/08/2026)

Quatrième passe. Après avoir validé la structure (§5undecies), j'ai vérifié les **invariants économiques** : chaque fonction qui déplace de l'argent conserve-t-elle la masse monétaire, et les soldes réels correspondent-ils au grand livre ? Migrations `0018_internal_transfer_fee_leak.sql` et le contrôle d'intégrité appliqués en live.

### Ce qui a été validé

- **14 fonctions déplacent de l'argent** (`_adjust_balance`). Somme des mouvements vérifiée à zéro pour chacune, branches conditionnelles comprises : `decide_market_purchase` a deux branches mutuellement exclusives (lingot vendu par la banque ou par un client), toutes deux équilibrées ; `decide_membership_request` et `finalize_manual_account_opening` posent le dépôt initial comme solde d'ouverture à la création du compte et débitent la trésorerie en regard.
- **Liens de notification** : les 24 chemins émis par les fonctions SQL pointent tous vers une route réelle, y compris le lien de messagerie construit dynamiquement (`'/' || rôle || '/messages/' || id`) — `create_message_thread` refusant explicitement un destinataire `prospect`, le rôle produit toujours une route qui existe.
- **Couverture RLS** : les 15 tables écrites directement par le frontend disposent toutes des policies correspondant aux opérations effectuées (un `upsert` PostgREST exigeant à la fois INSERT et UPDATE, vérifié). Une seule table a RLS actif sans aucune policy — `password_reset_codes`, refus par défaut voulu et documenté (migration 0009). Aucune table n'a RLS désactivé.

### FUITE MONÉTAIRE : la commission d'un virement interne était détruite

La somme de tous les soldes ne correspondait pas à la masse monétaire attendue. La réconciliation compte par compte contre le grand livre a isolé **-0,50 $** sur un compte client. Cause, dans `decide_transfer` :

```sql
v_fee := round(t.amount * v_fee_rate / 100, 2);            -- calculée TOUJOURS
perform _adjust_balance(t.recipient_account_id, t.amount - v_fee);  -- retirée MÊME en interne
if v_fee > 0 and not t.is_internal then
  perform _adjust_balance(v_bank_account, v_fee);          -- mais encaissée SEULEMENT si externe
end if;
```

Sur un virement interne, la commission était bel et bien retirée au destinataire mais **créditée à personne** : elle sortait du système. Trois conséquences simultanées — de la monnaie détruite à chaque virement interne ; le client payait une commission sur un virement entre ses propres comptes alors que tout le reste du code traite l'interne comme gratuit ; et la ligne de transaction affichait `fee_amount = 0` alors qu'une commission avait bien été prélevée, rendant les livres faux. Corrigé : la commission vaut zéro en interne, calculée une seule fois et utilisée partout, ce qui rend les trois mouvements cohérents par construction.

### Bug connexe : un virement d'un compte VERS LUI-MÊME était accepté

`submit_transfer` vérifiait l'appartenance du compte émetteur et l'existence du destinataire, mais jamais qu'ils sont différents. Le seul virement présent en production est exactement ce cas (50 $ envoyés d'un compte vers ce même compte) : opération sans aucun sens économique qui, combinée au bug ci-dessus, ne servait qu'à détruire la commission. Refusé désormais côté serveur, et signalé côté client avant l'envoi.

### Nouvel outil permanent : contrôle d'intégrité du grand livre

L'invariant qui a permis de trouver cette fuite n'existait nulle part — il a fallu le reconstruire à la main. Il est désormais installé comme fonction (`admin_check_ledger_integrity`, réservée à l'admin) et **affiché en haut du tableau de bord admin** : bandeau vert « Grand livre cohérent » ou tableau rouge détaillant chaque anomalie. Placé là volontairement plutôt que dans un écran dédié — une anomalie monétaire doit sauter aux yeux à la connexion, pas attendre qu'on pense à aller la chercher. Il détecte trois familles : transaction sans contrepartie, virement sur le même compte, solde qui ne correspond pas à l'historique des mouvements.

### Deux anomalies de DONNÉES à trancher (script écrit, volontairement NON appliqué)

Le contrôle révèle deux écarts hérités, dont les causes sont déjà corrigées dans le code mais dont les données n'ont jamais été rattrapées :

1. **3 004 531,00 $ créés depuis rien** — 9 dépôts d'ouverture des 17-18/08, antérieurs au correctif de la migration 0010 (§5bis-ter) qui a fait débiter la trésorerie. Le code a été corrigé le jour même, mais ces 9 dépôts n'ont jamais été payés par la banque. Les clients ont légitimement leur argent ; ce sont les livres de la banque qui sont faux.
2. **0,50 $ détruits** par la fuite décrite ci-dessus.

Le script `supabase/repairs/0001_repair_phantom_money.sql` corrige les deux, avec vérifications préalables et journalisation. **Il n'est pas appliqué, et il est rangé hors du dossier `migrations/`** pour ne jamais partir dans une application automatique : déplacer 3 M$ dans une économie de jeu de rôle en cours est une décision de game design, pas une décision technique. Trois options légitimes y sont documentées (tout réparer / ne rien faire / réparer la trésorerie seule).

### Incohérence : le « solde total » du client n'était pas celui de la banque

Le tableau de bord client additionnait **tous** les comptes, comptes clôturés compris, alors que la règle de solde minimum — celle qui autorise ou bloque virements et achats — s'appuie sur `client_total_balance()`, qui exclut les comptes clôturés. Le client pouvait donc se croire au-dessus du minimum requis et voir son virement refusé sans comprendre pourquoi. Le tableau de bord utilise désormais la valeur de la banque elle-même (repli sur un calcul local filtré à l'identique si l'appel échoue), et précise le nombre de comptes clôturés. Dans la foulée : les comptes gelés ou clôturés ne sont plus proposés comme compte débiteur ni destinataire dans l'écran de virement (la base les refuse de toute façon — les proposer ne menait qu'à un échec incompréhensible), et leur statut est affiché sur la fiche compte.

### Code mort traité

Deux exports n'étaient utilisés nulle part. Aucun n'a été supprimé — les deux méritaient d'être branchés :

- `getMyTotalBalance` : c'est précisément la valeur faisant autorité décrite ci-dessus. Désormais utilisée par le tableau de bord client.
- `deleteFeatureFlag` : `/admin/permissions` permettait de créer une fonctionnalité au registre mais jamais d'en retirer une. Bouton ajouté, interdit sur les fonctionnalités essentielles (`is_core`), avec un avertissement explicite : `has()` renvoyant `true` par défaut quand une clé est absente, supprimer une clé encore interrogée par le code rend l'écran concerné **définitivement visible** — l'inverse de ce qu'un admin attend. Le message oriente vers la case « Actif » pour un simple masquage.

### Piège d'ordre des migrations traité

La migration `0016` (écrite hier, toujours non appliquée) réécrit `submit_transfer` et aurait **effacé le correctif 0018** si elle était appliquée après. Le garde-fou anti-virement-sur-soi-même y a été reporté : les deux migrations peuvent désormais être appliquées dans n'importe quel ordre sans régression.

## 5undecies. Vérification automatisée code ↔ schéma, clôture des prêts, registre de fonctionnalités (25/08/2026)

## 5undecies. Vérification automatisée code ↔ schéma, clôture des prêts, registre de fonctionnalités (25/08/2026)

Troisième passe. Plutôt que de relire le code à l'œil, j'ai **extrait automatiquement tous les appels du frontend** (62 appels RPC, 34 tables, 15 jointures explicites, 14 implicites, 26 clés de fonctionnalités) et **confronté chacun au schéma réel de la base de production**. Migration `0017_loan_closure_and_irs_features.sql` appliquée en live.

### Ce que la vérification automatisée a validé (aucune action nécessaire)

- **62 appels RPC** : toutes les fonctions existent, et **aucun nom de paramètre ne diverge** de la signature réelle.
- **15 jointures PostgREST explicites** (`profiles!contrainte(...)`) : les 15 noms de contrainte existent bel et bien.
- **14 jointures implicites** : aucune n'est ambiguë (chacune a exactement une clé étrangère vers sa cible) — la famille de bug de §5sexies est bien refermée.
- **Toutes les colonnes** référencées en `select`/`eq`/`order`/`insert` existent, les trois exceptions ayant été corrigées à la passe précédente (§5decies).

Cette vérification est reproductible : c'est le filet qui manquait pour attraper les bugs de §5decies avant qu'ils n'atteignent la production.

### Bug trouvé : un prêt remboursé normalement n'était JAMAIS clôturé

`repay_loan_early` (remboursement anticipé) fait bien passer le prêt en `closed`. Mais le chemin **normal** — le client paie ses échéances une par une via la tâche planifiée quotidienne — ne le faisait nulle part : `repay_loan_installment_now` se contentait de décrémenter `outstanding_balance`.

Un client qui remboursait intégralement son prêt, dans les temps, se retrouvait donc avec un solde restant à 0, toutes ses échéances réglées… et un prêt éternellement `active`, `closed_at` à null. Il apparaissait indéfiniment endetté sur son propre écran (`/client/loans` continuait d'afficher le bouton « Solder ») comme au registre du personnel. Corrigé : la dernière échéance réglée clôture désormais le prêt, crédite la note de confiance, notifie le client et le personnel, et journalise l'opération. Requête de rattrapage incluse pour d'éventuels prêts déjà bloqués dans cet état.

Aucun prêt n'avait encore atteint ce stade en production (le seul prêt en base est toujours en attente d'approbation) : **la correction est préventive, mais le bug était certain dès le premier prêt mené à son terme.**

### Incohérences du registre de fonctionnalités — dans les deux sens

- **4 clés lues par le code mais jamais enregistrées** : `irs.clients.view`, `irs.accounts.view`, `irs.transactions.view`, `irs.gold.view`. Comme `has()` renvoie `true` par défaut quand la clé est absente, les 4 entrées du menu IRS s'affichaient toujours — et surtout **l'admin n'avait aucun moyen de les désactiver** depuis `/admin/permissions`, contrairement aux 3 autres interfaces. Le registre se voulait générique pour les 4 rôles ; l'IRS en était sorti par simple oubli d'insertion. Les 4 clés sont désormais au registre (activées par défaut : aucun changement de comportement, mais elles deviennent pilotables).
- **4 clés enregistrées mais lues nulle part** : `client.support`, `admin.gold.mint`, `admin.gold.edit_registry`, `admin.overrides.min_balance`. Un admin qui les désactivait voyait le réglage enregistré… sans le moindre effet. C'est pire qu'une bascule absente : l'interface affirmait une restriction qui n'existait pas. Les 4 pilotent désormais réellement leur écran (entrée Support du menu client ; bloc de frappe de lingot et édition du registre sur `/admin/gold` — qui bascule en lecture seule plutôt que de disparaître ; champ d'exception de solde minimum sur `/admin/clients`).

Piège évité au passage : rendre le bloc de frappe conditionnel exposait un `getElementById('mint-submit').addEventListener` sans `?.` — l'écran entier serait tombé sur un TypeError dès la fonctionnalité désactivée. Il reste **45 occurrences de ce motif** ailleurs dans le code ; toutes portent aujourd'hui sur des éléments rendus inconditionnellement (vérifié), mais c'est une fragilité latente : rendre un bloc conditionnel sans ajouter `?.` casse tout l'écran. À garder en tête avant chaque nouvelle bascule.

### Capacité backend enfin branchée

`profile_public_lookup(p_id)` existait en base depuis l'origine et n'était **appelée nulle part**. Elle résout précisément le problème identifié à la passe précédente : la policy `profiles_select` interdisant à un client de lire tout profil autre que le sien, une jointure vers le conseiller assigné renvoyait null sans erreur. Cette fonction `SECURITY DEFINER` ne divulgue que l'identité publique (jamais de donnée financière). **Le nom du conseiller Consulting Premium s'affiche donc enfin côté client**, avec un lien direct vers la messagerie pour le contacter.

### Autre correction d'affichage

Une échéance de prêt prélevée **avec** pénalité de retard est enregistrée en statut `late` (l'énumération `installment_status` ne connaît que `pending|paid|late`), ce qui affichait « En retard » sur une échéance pourtant bel et bien réglée, et pour toujours. La donnée distingue déjà les deux cas via `paid_at` : l'écran affiche désormais « Payée en retard », et détaille le montant de la pénalité. Choix délibéré de ne pas toucher à l'énumération — un `ALTER TYPE` est autrement plus risqué qu'un correctif d'affichage, pour un gain nul.

### Point de nommage trompeur (vérifié, aucun bug)

`permission_grants.account_id` porte une clé étrangère vers **`profiles(id)`**, pas vers `accounts(id)` — malgré son nom, et malgré la variable `selectedAccount` côté frontend qui contient en réalité un profil. J'ai vérifié les trois chemins (lecture dans `features.js`, lecture et écriture dans `/admin/permissions`) : ils sont **cohérents entre eux**, le système d'exceptions par client fonctionne. Aucun changement fait — renommer la colonne casserait les trois. Signalé ici pour qu'une future modification ne parte pas du mauvais présupposé.

## 5decies. Trois bugs silencieux côté client, visibilité des échecs, recherche & export (25/08/2026)

## 5decies. Trois bugs silencieux côté client, visibilité des échecs, recherche & export (25/08/2026)

Deuxième passe d'audit, menée après l'audit sécurité (§5nonies), sur demande de l'utilisateur de continuer à chercher ce qu'on peut améliorer. Cette fois la revue a porté sur la cohérence entre le code frontend et le schéma réel de la base, vérifiée requête par requête contre la base de production.

### Trois bugs réels trouvés — des écrans client vides en permanence

Vérification systématique de **chaque** couple (table, colonne de tri) utilisé par le frontend contre le schéma réel : 36 couples testés, 3 pointent vers une colonne qui n'existe pas.

- **L'historique des virements du client ne s'affichait jamais.** `getMyTransfers()` triait sur `transfers.created_at` — cette colonne n'existe pas, la date de dépôt s'appelle `requested_at`. PostgREST renvoyait une erreur explicite, avalée par le `.catch(() => [])` de l'écran, qui affichait alors « Aucun virement pour l'instant ». Il y a **1 virement réel en base** que son auteur n'a jamais pu voir.
- **Les demandes d'achat de lingot à la banque ne s'affichaient jamais** (`gold_bank_purchase_requests`, même cause exacte). **2 demandes réelles en base**, invisibles pour leurs auteurs.
- **Les demandes d'achat sur le marché de revente ne s'affichaient jamais** (`gold_market_purchase_requests`, même cause). Aucune donnée en base pour l'instant, donc jamais remarqué.

C'est exactement la même famille de bug que `listed_at` (trouvé et corrigé en 0012) et que les jointures ambiguës (§5sexies) : une erreur serveur parfaitement explicite, rendue invisible par le motif de capture d'erreur. Les trois écrans affichaient aussi une date vide (`t.created_at` inexistant côté rendu) — corrigé également.

### Cause profonde traitée : les échecs de chargement deviennent visibles

Le motif `.catch(() => [])` (130 occurrences) fait qu'un écran affiche **exactement le même état vide** qu'une requête réussie sans résultat et qu'une requête qui a échoué. Ni l'utilisateur ni le développeur ne peuvent faire la différence — c'est ce qui a permis aux trois bugs ci-dessus de survivre des semaines en production.

Nouveau module `src/lib/loadState.js` : `loadAll()` charge un ensemble de requêtes nommées sans jamais faire tomber l'écran (le comportement pratique est conservé), mais **journalise chaque échec en console avec le nom de la requête** et signale à l'écran qu'il doit prévenir l'utilisateur — bandeau discret et non bloquant « Certaines données n'ont pas pu être chargées », avec bouton Réessayer. Appliqué aux écrans client `/client/accounts` et `/client/transfers` ; à étendre progressivement aux autres écrans (le motif reste en place ailleurs, sans régression).

### Évolutions

- **Recherche et filtre par type d'opération sur `/client/accounts`** : le personnel disposait de ces filtres sur son historique de transactions depuis le lot du 20/08, mais le client n'avait rien sur ses propres opérations. Filtrage instantané côté navigateur sur les opérations déjà chargées, sans aller-retour réseau — ce qui compte dans le navigateur intégré du jeu.
- **Export CSV du relevé** sur `/client/accounts` (avec BOM UTF-8, sinon Excel affiche les accents en mojibake) ; l'export respecte le filtre actif. Limite de chargement relevée de 30 à 200 opérations.
- **Taux d'épargne affiché sur la carte du compte épargne** : le taux et son activation étaient pilotables depuis `/admin/economic-settings` depuis l'origine, mais le titulaire d'un compte épargne n'avait aucun moyen de connaître sa rémunération. Affiche le taux, et précise explicitement quand les versements sont suspendus (`savings_interest_enabled` à faux, ce qui est le cas par défaut).
- **Table des libellés de type d'opération centralisée** dans `src/lib/format.js` (`txTypeLabel`) : elle vivait dans `transactionsScreen.js`, donc n'était disponible que pour le personnel. Complétée au passage des types manquants (commissions de virement et de marché, pénalité de retard, ajustement bancaire).

### Écrit mais NON appliqué — en attente de ton feu vert

La migration `0016_transfer_caps_scheduled_transfers_documents.sql` est **écrite et versionnée dans le dépôt, mais n'a pas été appliquée à la base** : le garde-fou de sécurité de mon environnement a bloqué l'écriture (création de table + modification de policies sur une base de production). Elle contient trois évolutions issues des recommandations de §5nonies :

1. **Plafonds de virement** (`max_transfer_amount` par opération, `max_daily_transfer_total` sur 24 h glissantes) — jusqu'ici seul un *minimum* existait, et `fraud_unusual_transfer_amount` ne fait que signaler a posteriori sans rien bloquer : un compte compromis peut vider tous les comptes en une opération. Pilotables globalement et par client via le mécanisme d'exception existant ; 0 = illimité, donc sans effet tant qu'aucune valeur n'est posée. Le frontend est déjà prêt (affiche le plafond sous le champ montant s'il est configuré, ne montre rien sinon) — il fonctionne donc correctement avec ou sans la migration.
2. **Virements permanents** (`scheduled_transfers` + tâche `pg_cron` quotidienne). Choix d'architecture délibéré et documenté dans la migration : l'échéance ne débite **jamais** directement, elle dépose une demande de virement ordinaire que le personnel valide comme les autres. Un virement permanent auto-exécuté créerait un contournement permanent du principe central du projet (aucun mouvement de fonds sans validation) — un client pourrait le programmer pour qu'il s'exécute quand aucun employé n'est connecté.
3. **Documents / relevés** : la table `documents` et ses policies existent depuis la migration 0001b et l'écran `/client/documents` sait les afficher depuis la phase 3, mais **aucun bucket Storage n'a jamais été créé et aucune interface ne permet d'en émettre** — le message « Vos relevés apparaîtront ici » ne peut donc jamais se réaliser. La migration crée le bucket, ses policies (personnel écrit, client lit uniquement ses propres fichiers via le préfixe de chemin) et les fonctions d'émission/suppression. L'interface d'upload côté personnel reste à construire une fois la migration passée.

Pour appliquer : coller le contenu du fichier dans l'éditeur SQL du tableau de bord Supabase, ou me redemander de le faire en validant l'action. **Le reste du lot ne dépend pas de cette migration** et peut être mis en ligne immédiatement.

### Recommandation non traitée, à ne pas perdre de vue

`create_scheduled_transfer` et le nom du conseiller consulting côté client ont un point commun révélateur : la policy `profiles_select` (`id = auth.uid() or is_staff()`) empêche un client de lire le moindre profil autre que le sien. Toute information sur un tiers (nom du conseiller assigné, titulaire d'un compte destinataire) doit donc passer par une fonction `SECURITY DEFINER` dédiée, comme le fait déjà `resolve_account_by_iban`. C'est la bonne architecture, mais elle impose de créer une fonction par besoin — à garder en tête avant de promettre un affichage côté client.

## 5nonies. Audit sécurité complet du projet + correctifs (25/08/2026)

## 5nonies. Audit sécurité complet du projet + correctifs (25/08/2026)

Revue de l'ensemble du projet menée de ma propre initiative (code frontend, policies RLS, fonctions SQL, avis de sécurité Supabase), à la demande de l'utilisateur de « regarder tout le projet et voir ce qu'on peut ajouter ». Migration `0015_security_hardening_followup.sql` appliquée en live.

**Corrigé dans ce lot (sécurité, haute priorité) :**

- **Faille XSS stockée dans le panneau de notifications** : `create_message_thread` est appelable par n'importe quel utilisateur authentifié (y compris un client) vers un employé/admin/IRS, et insère le sujet/corps du message tels quels dans `notifications.title`/`body`. `src/lib/shell.js` les affichait sans `escapeHtml` (déjà importé et utilisé ailleurs dans le même fichier, juste oublié à cet endroit) — un client malveillant pouvait donc exécuter du HTML/JS arbitraire dans le navigateur d'un admin dès la réception de la notification, sans même que la victime ouvre le message. Corrigé.
- **`economic_settings` devenu lisible en entier par un visiteur anonyme** : la policy ouverte par la migration 0014 pour le mode maintenance/bannière (`using (true)`) exposait par erreur toute la table (taux de prêt/épargne, plafonds, seuils de détection de fraude, cours de l'or) à n'importe qui, même sans compte. Restreint aux deux seules clés concernées (`maintenance_mode`, `announcement_banner`), le reste redevient réservé aux utilisateurs connectés.
- **`gold_bars` lisible par tout utilisateur connecté, y compris les lingots des autres clients** : la policy (`auth.uid() is not null`) n'appliquait aucune restriction de propriétaire — un client pouvait interroger directement la table via l'API REST et voir le patrimoine en lingots de tous les autres clients. Restreint au propriétaire, au stock banque, et au personnel.
- **22 fonctions `SECURITY DEFINER` exécutables par un visiteur anonyme (rôle `anon`), non authentifié** : le durcissement global des permissions d'exécution (migrations 0006/0006b : `revoke ... from public/anon` puis `grant ... to authenticated`) est un instantané figé au moment où il a tourné — chaque fonction créée depuis (migrations 0007 à 0014) a hérité du comportement par défaut de PostgreSQL (`EXECUTE` accordé à `PUBLIC`, donc à `anon`), confirmé par les avis de sécurité Supabase. Concerné notamment : `admin_delete_account`, `admin_adjust_account_balance`, `staff_decide_safe_request`, `admin_update_transaction_description`, `create_message_thread`, `upsert_client_info`. Ces fonctions vérifient toutes le rôle en interne, donc aucun appel anonyme ne pouvait aboutir — mais l'exposition elle-même n'était pas voulue. Le rattrapage (`revoke`/`grant` réappliqué à tout le schéma) est désormais systématique à chaque nouvelle migration à surveiller pour l'avenir.
- **`search_path` manquant sur `charge_account_fees()`** : seule fonction oubliée lors du durcissement initial (recréée après coup par la migration 0012 sans la clause `set search_path`).
- Vérifié après coup via les avis de sécurité Supabase : les 3 points ci-dessus (fonctions anon, search_path) n'apparaissent plus. Restent seulement des avis informatifs attendus par construction (fonctions exécutables par `authenticated` — c'est le principe de l'architecture) et `password_reset_codes` sans policy (deny-by-default voulu et documenté, migration 0009).
- **Non corrigible depuis cet environnement** : protection Supabase Auth contre les mots de passe compromis (HaveIBeenPwned) désactivée — à activer côté utilisateur dans Authentication > Attack Protection sur le tableau de bord Supabase.

**Ajouté dans ce lot (petites évolutions, à faible risque) :**

- **Le client peut désormais retirer lui-même une annonce de vente de lingot** (nouvelle fonction `cancel_market_listing`, bouton « Retirer » sur `/client/gold/market`) — jusqu'ici seul le personnel pouvait annuler une annonce, le client devait passer par le support pour retirer son propre bien de la vente.
- **Filet de sécurité dans le routeur** (`src/lib/router.js`) : une exception non interceptée dans un écran laissait l'utilisateur bloqué indéfiniment sur « Chargement… » sans aucun message (aggravé par l'absence des dialogues natifs, neutralisés pour compatibilité CEF depuis le lot du 20/08). Affiche désormais un écran d'erreur générique avec un bouton de retour à l'accueil, et journalise les rejets de promesse non interceptés en console pour faciliter le diagnostic futur.
- **Export CSV du relevé de transactions** sur `/client/accounts` (généré côté client à partir des transactions déjà chargées, aucune dépendance serveur ; limite de chargement relevée de 30 à 200 lignes pour un export plus complet).

**Recommandations identifiées mais non traitées dans ce lot (à valider avec l'utilisateur avant implémentation — plus structurantes ou plus coûteuses en temps)** :

- *Priorité moyenne* : aucune pagination sur les listes qui grossiront avec l'usage réel (transactions client, recherche client personnel, historique transactions personnel) ; aucun export/relevé PDF officiel (la table `documents` existe en base avec RLS complet mais aucune UI ni bucket Storage ne permet d'en émettre) ; aucun plafond de virement (seulement un minimum) ; aucune recherche/filtre sur l'historique de transactions côté client (le personnel en a un, pas le client) ; nom du conseiller consulting jamais affiché côté client.
- *Priorité basse* : labels de formulaire non liés à leur champ (`for`/`id`) sur la majorité des écrans admin/employé (accessibilité) ; pas de spinner visuel (texte « Chargement… » seul) ; édition JSON brute pour les paramètres économiques/CMS sans validation de champ ; pas de virements programmés/récurrents ; taux d'épargne non affiché sur la fiche compte épargne côté client ; IRS sans vue dédiée aux prêts (visibles via l'historique transactions filtrable, juste sans écran propre).

## 5octies. Bug pilotage économique/config système, comptes, coffres, onglet Infos, maintenance réelle, messagerie (24/08/2026)

Troisième gros lot de correctifs/évolutions remonté par l'utilisateur (23 points). Migration `0014_third_batch_fixes_features.sql` appliquée en live, ainsi que l'Edge Function `create-account` (déployée directement via l'API Supabase, sans passer par GitHub Pages).

- **Bug racine trouvé et corrigé : « ça se bloque » sur Pilotage économique, avec l'erreur exacte `null value in column "label"`** — reproduit en direct sur la base : Postgres valide les contraintes NOT NULL de la ligne candidate d'un `INSERT ... ON CONFLICT DO UPDATE` **avant** même de vérifier s'il y a conflit. Un upsert qui n'envoie que `{key, value}` (sans `label`/`value_type`/`category`) échoue donc systématiquement, même pour mettre à jour une ligne déjà existante. Corrigé dans `admin/economic-settings.js` **et** dans les 3 mêmes points de `admin/system.js` (maintenance, bannière, autres paramètres) — cette dernière page était très probablement la cause de « l'onglet configuration système marche pas ».
- **Mode maintenance et bannière d'annonce : effet réel enfin implémenté** (jusqu'ici enregistrés en base mais lus par aucun écran). La bannière s'affiche désormais en haut de l'accueil public et des 4 interfaces internes. Le mode maintenance suspend l'ouverture de nouveaux comptes (accueil public + `/signup`) et affiche une notice sur toutes les interfaces sauf admin (jamais bloqué, pour toujours pouvoir désactiver le mode). Nouveau module `src/lib/systemSettings.js`. Pour du contrôle plus fin par fonctionnalité/interface, `/admin/permissions` (déjà existant) reste la solution.
- **Ajustement manuel du solde d'un compte + suppression de compte** : nouvelles fonctions `admin_adjust_account_balance` (contrepartie sur les fonds propres, ligne dans l'historique — `tx_type = 'admin_adjustment'`, prévu dans le schéma depuis l'origine mais jamais implémenté) et `admin_delete_account` (suppression définitive refusée si solde non nul ou historique de transactions — utiliser le statut `closed` sinon). Boutons ajoutés sur `/admin/clients`.
- **Numéro de téléphone** : nouvelle colonne `profiles.phone_number`, collecté obligatoirement à l'inscription (comme l'ID Discord), éditable dans les Paramètres des 4 rôles, visible sur la fiche client (`/admin/clients`, `/employee/clients`) pour que le personnel puisse joindre ses clients. Numéros du banquier (Salomon Newman) renseignés directement en base.
- **Lien Discord affiché en clair** : remplacement de la copie automatique au clic (`src/lib/externalLink.js`) par un simple affichage du lien en texte sélectionnable sous chaque bouton/lien Discord, pour une copie manuelle par le joueur.
- **Ouverture de compte au guichet — sélection d'un profil existant** : la fonction serveur acceptait déjà n'importe quel profil (pas seulement un prospect), mais il fallait taper l'identifiant exact. Remplacé par un sélecteur de recherche en direct (nom ou identifiant), fonctionnant aussi bien pour un prospect auto-inscrit que pour un client déjà titulaire d'un compte (lui ajoute alors un compte supplémentaire) — `employee/account-opening.js` et `admin/account-opening.js`.
- **Coffres-forts — décision simple en une étape** : nouvelle fonction `staff_decide_safe_request` (autoriser/refuser directement, coffre choisi automatiquement si non précisé) en plus du flux existant (programmer un rendez-vous → confirmer), conservé en option repliable. Le prélèvement hebdomadaire automatique et l'édition du tarif existaient déjà (migration `0013`) ; le tarif est désormais éditable aussi directement depuis Pilotage économique.
- **Onglet « Infos » côté client** : nouvel onglet en lecture seule, éditable par l'admin et l'employé (fiche client) et par l'IRS (exception d'écriture ciblée — le seul endroit de toute l'interface IRS où une écriture est possible). Nouvelle table `client_info_notes` + fonctions `get_client_info`/`upsert_client_info`.
- **Masquage par interface — sélection au lieu de saisie d'UUID** : `/admin/visibility` propose désormais une recherche de client puis une liste de ses comptes/transactions récentes à choisir directement, en plus de la saisie manuelle d'UUID conservée en repli.
- **Édition d'une transaction (admin)** : `/admin/transactions` permet désormais de corriger la description d'une transaction — jamais le montant, les comptes ou le statut d'une transaction validée (intégrité comptable).
- **Décision admin directe des prêts en attente** : `/admin/loans` permet maintenant d'approuver/refuser une demande dès son dépôt, sans attendre qu'un employé la réceptionne d'abord (l'admin ayant déjà toutes les permissions du personnel) — la fonction serveur le permettait déjà, seul le bouton manquait.
- **Matrice de messagerie resserrée** : Client → Employé/Admin uniquement (plus l'IRS) ; IRS → Employé/Admin uniquement (plus les clients ni d'autres IRS) ; Admin/Employé → tout le monde (inchangé). Appliqué à la fois dans la liste de contacts et dans la fonction d'envoi (`list_messageable_contacts`/`create_message_thread`).
- **Alerte fraude automatique sur solde négatif** : la fonction `_adjust_balance` (point de passage unique de tout mouvement d'argent) crée désormais une alerte fraude automatique au moment précis où un compte client (hors trésorerie banque) bascule sous zéro — une seule fois, pas à chaque nouvelle opération tant qu'il le reste.
- **Enregistrement des nouveaux écrans dans le registre de fonctionnalités** : `employee.transactions.view`, `admin.transactions.view`, `client.info.view` — désormais activables/désactivables comme le reste depuis `/admin/permissions`.
- **Points vérifiés sans modification nécessaire** : le décaissement d'un prêt crédite bien le compte client et débite les fonds propres de la banque (et inversement pour les remboursements/pénalités) — déjà correct. La couverture des frais (commission de virement, commission de marché, frais de gestion, frais de dossier de prêt) débite déjà systématiquement le client et crédite la banque sur chaque type de transaction concerné — aucun mécanisme de frais supplémentaire ajouté pour éviter un doublon. La correction manuelle du rapport de caisse (`/admin/cashier`) existait déjà. Le composeur de messagerie sans bouton « Nouveau message » (corrigé au lot précédent) est bien en place dans le code actuel — si le blocage persiste après un rafraîchissement complet, il s'agit probablement d'un autre chemin non identifié, à décrire précisément pour investigation.
- **Marché de revente de lingots (« le client voit des lingots jamais mis en vente », « l'admin ne voit pas le marché »)** : le design à deux vendeurs (banque après frappe, ou propriétaire après achat) est bien celui voulu et déjà implémenté (migration `0012`) ; le code de `/admin/gold` affiche bel et bien la section « Lingots actuellement en vente ». La table `gold_market_listings` est actuellement vide en base — impossible de reproduire un bug avec ces données. Probable écran resté en cache avant le lot précédent : à revérifier après un rafraîchissement complet.

## 5septies. Refus avec motif généralisé, frais de dossier, coffres hebdomadaires, historique transactions, vue 360 (20/08/2026)

Deuxième gros lot de correctifs/évolutions remonté par l'utilisateur (13 points + 3 précisions sur captures d'écran). Migration `0013_reject_paths_fees_safes_transactions.sql` appliquée en live.

- **Freeze de la messagerie en jeu (bug majeur) — cause probable identifiée et corrigée** : le navigateur intégré de FiveM (CEF) ne rend jamais les boîtes de dialogue JavaScript natives (`alert`/`confirm`/`prompt`) — un appel à l'une d'elles bloque le thread JS en attendant une réponse qui n'arrivera jamais, gelant toute l'interface. C'est très probablement la cause de « quand j'appuie sur messagerie en jeu ça freeze l'interface » (et potentiellement d'autres blocages signalés comme « ça marche pas » sans message d'erreur). Remplacement systématique des 105 appels natifs par des équivalents non-bloquants en pur DOM (`src/lib/uiDialogs.js` : `showAlert`/`showConfirm`/`showPrompt`) dans les 34 fichiers concernés — vérifié par recherche exhaustive (plus aucun appel natif dans `src`) et par une compilation réussie. **À retester en jeu pour confirmer** : je n'ai pas d'accès réseau en jeu depuis cet environnement pour reproduire le freeze moi-même, donc je ne peux pas garantir à 100 % que c'était l'unique cause, mais c'est le point d'incompatibilité CEF le plus net trouvé dans le code.
- **Messagerie — liste de contacts affichée directement** : le composeur « Nouveau message » (`src/pages/shared/messagesScreen.js`) s'affiche désormais directement sur l'écran avec la liste de contacts déjà chargée, sans bouton ni fenêtre modale intermédiaire à ouvrir au préalable.
- **Refus avec motif pour toutes les demandes client** : ajouté pour les deux types qui ne l'avaient pas encore — coffres-forts (`reject_safe_request`, boutons « Refuser » dans `/admin/safes` et `/employee/safes`) et consulting premium (`reject_consulting_request`, boutons « Refuser » dans `/admin/consulting` et `/employee/consulting`). Les autres types (virements, achats de lingots banque/marché, prêts, adhésions) disposaient déjà de cette capacité. Le motif de refus est visible côté client sur chaque écran concerné.
- **Frais de dossier configurables** : nouveau paramètre `loan_processing_fee` dans `/admin/economic-settings` (catégorie « frais », 5 000 $ par défaut), prélevé automatiquement au décaissement d'un prêt validé, en plus du capital — apparaît comme une ligne de transaction séparée (`loan_processing_fee`) dans l'historique.
- **Coffres-forts — passage à un tarif hebdomadaire configurable** : la colonne `annual_fee` devient `weekly_fee` ; la confirmation de location facture désormais une semaine (et non plus une année complète), et une nouvelle tâche planifiée quotidienne (`charge_safe_weekly_fees`, comme les frais de gestion de compte) prélève automatiquement le loyer chaque semaine tant que le coffre reste loué. Nouvel écran de gestion du parc sur `/admin/safes` : création de nouveaux coffres et modification du tarif hebdomadaire de chaque coffre existant.
- **Lingots mis en vente par les clients, visibles côté employé** : `/admin/gold` avait déjà ça depuis le lot précédent (section « Lingots actuellement en vente », avec retrait possible) — ajouté également sur `/employee/gold`, qui n'avait que les files d'attente d'achat.
- **`/admin/gold` réorganisé** (retour sur capture d'écran : « trop répétitif ») : les deux panneaux quasi identiques « Achats banque » et « Achats sur le marché de revente » ont été fusionnés en une seule liste « Demandes d'achat en attente », triée par date, avec une étiquette indiquant la nature de chaque demande — même contenu, moins de répétition visuelle. Répliqué sur `/employee/gold`.
- **Historique des transactions côté personnel** : nouvel écran `/admin/transactions` et `/employee/transactions` (nouvelle fonction `staff_list_transactions`), avec recherche texte, filtre par type de service et filtre par catégorie de clientèle (VIP, Entreprise, etc.) — jusqu'ici seul le rôle IRS disposait d'un tel registre.
- **Tableau de bord — vue 360** : ajout d'une carte « Consulting en attente » et d'un tableau « Aperçu global » listant les 12 dernières demandes en attente tous types confondus (adhésions, virements, coffres, prêts, consulting, support, fraude), cliquables, sur `/admin` et `/employee`.
- **ID Discord obligatoire à l'inscription** : le champ n'est plus facultatif sur `/signup` (`required` + validation côté serveur dans `signUpProspect`), avec une note explicative précisant que c'est le seul moyen de réinitialiser un mot de passe oublié.
- **Page mot de passe oublié** : « Hurricane FA » remplacé par « Newman Bank », avec un lien Discord copiable directement sous le texte (capture d'écran 1).
- **Journalisation (`log_audit`)** : deux trous comblés — la prise en charge d'un virement (`claim_transfer`) et la prise en charge d'une demande de coffre (`claim_safe_request`) n'étaient pas tracées (les décisions finales l'étaient déjà). Passe non exhaustive — il peut rester d'autres actions mineures non tracées, à signaler si repérées.
- **Dépôt initial à l'inscription débité de la trésorerie + visible en transaction** : vérifié — c'était déjà correctement implémenté (migration `0010`), aucun changement nécessaire.

### Points de la demande déjà couverts par un lot précédent (aucune action nécessaire)
« Possibilité de retirer un lingot du marché côté admin » et « les lingots en vente côté client doivent apparaître côté employé/admin » étaient déjà résolus par la migration `0012` (section admin) ; ce lot-ci a complété la partie employé qui manquait encore.

## 5sexies. Correctifs et évolutions — retour utilisateur du 19/08/2026 (soir)

Lot de 9 points remontés par l'utilisateur en une fois, traités ensemble :

- **Bug majeur trouvé et corrigé : les tickets de support et les demandes d'achat de lingot n'apparaissaient plus côté employé/admin** (et plusieurs autres écrans avec eux — file d'accueil, coffres-forts, prêts, consulting, alertes fraude, comptes IRS, registre des lingots). Cause : plusieurs tables ont **plusieurs** clés étrangères vers `profiles` (ex: `support_tickets.client_id` **et** `support_tickets.assigned_to` ; `gold_bank_purchase_requests` en a même 4). Les requêtes `.select('*, profiles(...))')` sans préciser laquelle utiliser sont devenues ambiguës pour PostgREST dès que ces colonnes existent, et l'appel échouait silencieusement (erreur avalée, écran vide). Corrigé dans `src/lib/employeeApi.js` et `src/lib/adminApi.js` en précisant explicitement la relation à utiliser (`profiles!nom_de_la_contrainte(...)`) sur les 10 requêtes concernées. Au passage, un deuxième bug de la même veine a été trouvé et corrigé sur `/client/gold/market` (`src/lib/clientApi.js`) : la liste des lingots en vente et « mes lingots en vente » triaient sur une colonne `listed_at` qui n'existe pas (la vraie colonne est `created_at`) — la requête échouait aussi silencieusement.
- **Clic sur une notification renvoyant à l'accueil** : les fonctions d'émission avaient été corrigées le 18/08 (migration `0010`), mais les notifications déjà en base à ce moment-là gardaient leur ancien lien cassé (ex: `/admin/operations/loans`, une route qui n'a jamais existé). Nettoyées en base par la migration `0012` (toutes les notifications existantes avec un lien de l'ancien format ont été réécrites vers le nouveau).
- **Bouton Discord inopérant en jeu** : le navigateur intégré de FiveM bloque silencieusement l'ouverture d'un nouvel onglet externe (`target="_blank"`), donc cliquer « ne fait rien » en jeu alors que ça fonctionne normalement hors jeu. Ajout d'un repli universel (`src/lib/externalLink.js`) : chaque lien Discord (sidebars des 4 interfaces, `/login`, page d'accueil) copie désormais systématiquement l'URL dans le presse-papiers au clic (avec confirmation visuelle), en plus de tenter l'ouverture normale — ce qui permet au joueur en jeu de coller le lien ailleurs (client Discord, navigateur Steam). Le libellé a aussi été changé de « Rejoindre le Discord » à **« Discord Newman Bank »** partout (le lien mène au Discord de la banque, pas au serveur Hurricane FA en tant que tel).
- **Messagerie — choix précis du destinataire** : la possibilité de choisir un employé ou un admin spécifique (côté client), et pour le personnel (IRS/Employé/Admin) de se contacter entre eux, existait déjà côté serveur (`list_messageable_contacts`/`create_message_thread` n'interdisent que client↔client) mais n'était pas mise en valeur dans l'interface — la liste de contacts était plate, sans distinction claire des rôles. Le sélecteur du composeur (`src/pages/shared/messagesScreen.js`) regroupe désormais les contacts par rôle (`<optgroup>` Client / Employé / Admin / IRS), ce qui rend explicite qu'on peut cibler précisément qui on veut, y compris un collègue d'un autre rôle.
- **Frais de gestion en pourcentage** : `account_fee_amount` (montant fixe) remplacé par `account_fee_percent` (pourcentage du solde du compte, prélevé selon la fréquence déjà existante `account_fee_frequency_days` — cette fréquence était déjà pilotable depuis `/admin/economic-settings`, ça n'a pas changé). `charge_account_fees()` calcule désormais le prélèvement comme `solde × pourcentage / 100`, sans frais sur un compte à zéro ou déjà négatif. Valeur par défaut posée à 1 % — à ajuster depuis `/admin/economic-settings` (catégorie « frais »).
- **Mise en vente de lingots côté admin** : nouvelle section sur `/admin/gold` (« Mettre un lingot en vente sur le marché » + « Lingots actuellement en vente ») permettant de lister n'importe quel lingot disponible (« in_vault ») sur le marché de revente comme un client le ferait pour le sien, et de suivre/retirer les annonces actives — jusqu'ici seul un client pouvait mettre en vente son propre lingot. Nouvelles fonctions `admin_create_market_listing`/`admin_cancel_market_listing` (migration `0012`) ; `gold_market_listings.seller_client_id` est désormais nullable pour représenter un lingot vendu directement par la banque (le prix intégral revient alors à la trésorerie, sans commission de marché).

## 5quinquies. Fusion inscription + demande d'adhésion en une seule étape (19/08/2026)

Retour utilisateur : le parcours en deux étapes distinctes (1. `/signup` crée juste un identifiant/mot de passe puis renvoie vers `/login` ; 2. une fois reconnecté, l'écran `/prospect` propose de soumettre la demande d'adhésion) existait depuis la toute première version du site (Phase 1/3, confirmé par l'historique git — ce n'est pas un changement introduit par un lot précédent) mais ne correspondait pas au fonctionnement voulu : une seule étape, avec le message "Demande en cours d'examen" affiché immédiatement après l'inscription.

`/signup` (`src/pages/auth/signup.js`) intègre désormais directement les champs de la demande d'adhésion (type de compte, dépôt initial, motivation) en plus des champs de création de compte. À la soumission : création du compte (`signUpProspect`, session ouverte immédiatement car la confirmation par e-mail est désactivée côté Supabase) puis, dans la foulée, envoi de la demande d'adhésion (`submitMembershipRequest`) — puis redirection vers `/prospect`, qui affiche directement l'écran d'attente "Demande en cours d'examen" (aucun passage par `/login` entre les deux). Si la création du compte réussit mais l'envoi de la demande échoue (cas limite), l'utilisateur est renvoyé vers `/prospect` où le formulaire de demande reste disponible pour réessayer manuellement — le compte n'est jamais perdu.

L'écran `/prospect` et son formulaire (`src/pages/client/membership-request.js`) restent inchangés et toujours utiles : ils servent de filet de sécurité pour les anciens comptes prospects créés avant ce correctif, et pour la resoumission d'une demande refusée (`existing.status === 'rejected'`).

## 5quater. Retrait du CAPTCHA Cloudflare Turnstile — incompatibilité CEF (19/08/2026)

Incident en production : une fois « Bot and Abuse Protection » activé côté Supabase (Authentication > Attack Protection), **toutes les connexions et inscriptions depuis le navigateur intégré de FiveM (CEF) ont commencé à échouer** avec l'erreur Cloudflare `600010` ("bot behavior detected"), alors que ça fonctionnait normalement depuis un navigateur classique (Chrome/Edge). Diagnostic confirmé via les logs Supabase (`captcha protection: request disallowed`) puis via un ticket officiel du dépôt CEF ([chromiumembedded/cef#3547](https://github.com/chromiumembedded/cef/issues/3547), fermé "not planned") : le moteur CEF échoue structurellement aux vérifications d'intégrité de navigateur de Cloudflare — ce n'est pas un problème de configuration côté Newpad, et ça ne peut pas être corrigé en ajustant le widget.

Turnstile a donc été entièrement retiré (`src/lib/turnstile.js` supprimé, widgets retirés de `/login` et `/signup`, clé `VITE_TURNSTILE_SITE_KEY` inutilisée) et remplacé par un **honeypot** : un champ de formulaire invisible (`website`, positionné hors écran, jamais rempli par un humain) transmis dans les métadonnées d'inscription et vérifié côté serveur dans le trigger `handle_new_auth_user()` (migration `0011_honeypot_signup_guard`) — si le champ est renseigné, la création du compte est rejetée avant même l'insertion du profil. Fonctionne identiquement en jeu et hors jeu, sans dépendance à un service externe. La protection anti-abus « Bot and Abuse Protection » a été désactivée côté tableau de bord Supabase (elle est maintenant inutile et bloquerait à nouveau les joueurs en jeu si réactivée).

## 1. Inventaire (résumé de cadrage)

- **4 rôles + accueil public** : Client, Employé, Admin (= employé + pouvoirs élargis), IRS (staff Hurricane FA, lecture seule garantie en base). Une seule base de données, un seul déploiement.
- **Authentification** : identifiant + mot de passe, e-mail synthétique `identifiant@newpad.local` en interne (jamais montré). Prospect → inscription libre → demande d'adhésion → activation automatique du compte client à la validation employé/admin. Employé/admin/IRS créés manuellement.
- **Client** : dashboard, infos ville, comptes multiples (IBAN, solde), virements (≥ 100 000 $ sauf entre ses propres comptes), transactions, bénéficiaires, lingots d'or (achat banque + marché de revente), coffres-forts, prêts professionnels, consulting premium, documents, support (tickets = conversation), paramètres.
- **Employé** : dashboard, recherche/registre clients (catégories dynamiques), ouverture de compte guichet, demandes d'adhésion, file clients, traitement virements/prêts (réception seulement)/coffres/lingots, rapports de caisse (lecture), alertes fraude, support, journal d'activité.
- **Admin** : tout ce que fait l'employé (+ correction manuelle du rapport de caisse) + validation finale des prêts + frappe/édition directe du registre de lingots + autorisation des opérations sous le solde minimum + gestion des employés/comptes clients/permissions génériques par compte/comptes IRS/masquage compte-ou-transaction par interface + pilotage économique complet (tous les seuils, avec exceptions par client) + contenu de tout le site (CMS) + audit & sécurité + rapports & statistiques + configuration système (maintenance, bannière, activation des fonctionnalités).
- **IRS** : interface dédiée à part entière (comme les 3 autres), lecture seule sur les transactions/comptes/stats agrégées, restriction garantie par RLS (aucun grant d'écriture, aucun accès direct aux tables de base — uniquement via des fonctions `irs_*` dédiées), soumise au masquage par interface.
- **Processus métier communs** : demande → notification → "en cours de traitement" → décision. Aucune saisie manuelle de solde nulle part : chaque mouvement de fonds découle exclusivement de la validation d'une action prévue par le système, via des fonctions Postgres atomiques.
- **Pilotage économique 100% dynamique** : solde minimum (global + exception par client), montant minimum de virement, fourchette de prix marketplace lingots, taux prêt/épargne/pénalité de retard, cours de l'or, frais et commissions — tout en base, rien codé en dur.
- **Contraintes techniques** : Supabase (Postgres + Auth + Realtime + Storage + pg_cron), compatibilité navigateur intégré FiveM, déploiement GitHub Pages, RLS pour toutes les restrictions d'accès (y compris IRS et masquage).

## 2. Découpage en phases

| Phase | Contenu | Statut |
|---|---|---|
| 0 | Cadrage, inventaire, décisions infra (Supabase connecté, format multi-fichiers) | ✅ Fait |
| 1 | Fondations : schéma DB complet, RLS, fonctions métier, socle frontend (design system, routing, auth) | ✅ Déployé sur le vrai projet Supabase (`newpad-bnw-v2`, réf. `vdbuvltfwulsxqjpwzhr`, région Paris — voir incident et redéploiement §3). Socle frontend fonctionnel et reconnecté (`.env.production` mis à jour sur GitHub, "Confirm email" désactivé sur le nouveau projet — site de nouveau opérationnel pour l'auth) |
| 2 | Page d'accueil publique | ✅ Écran construit, relié au vrai projet Supabase (`site_content`, piloté depuis l'admin une fois Phase 5 construite ; fallback conservé si hors-ligne) |
| 3 | Interface Client (tous les écrans) | ✅ Construite — 11 écrans (voir §4). Livrée via zip pour upload manuel GitHub. |
| 4 | Interface Employé | ✅ Construite — 16 écrans (voir §4). Ouverture de compte au guichet désormais pleinement fonctionnelle (Edge Function `create-account` déployée, voir §5). Livrée via zip pour upload manuel GitHub. |
| 5 | Interface Admin | ✅ Construite — 23 écrans (voir §4). Livrée via zip pour upload manuel GitHub. |
| 6 | Interface IRS | ✅ Construite — 6 écrans, strictement lecture seule (voir §4). Livrée via zip pour upload manuel GitHub. |
| 7 | Automatisations & temps réel (notifications, Realtime, tâches planifiées) | 🟡 Fonctions et jobs `pg_cron` écrits côté DB (0002/0005) ; notifications realtime branchées côté frontend pour les 4 rôles (`src/lib/notifications.js`) et déjà vérifiées visuellement pour Client/Employé ; reste à vérifier en conditions réelles pour Admin/IRS une fois le site utilisé en jeu |
| 8 | Données de démo, tests, déploiement GitHub Pages | 🟡 Déploiement GitHub Pages fait et vérifié en ligne (`bouzoumacabre.github.io/newpad`) ; données de démo et tests de bout en bout restent à faire |

## 3. Base de données — ce qui est fait

> ⚠️ **Incident du 15/08/2026** : le projet Supabase d'origine (`newpad-bnw`, réf. `hvtonptowwriprbmmzmw`) a été supprimé par erreur (nettoyage voulu d'un autre projet, obsolète et sans rapport — `NEWMAN BANK` — mais suppression du mauvais projet). Aucune donnée réelle de jeu n'était encore en base (uniquement les données de référence de la migration 0004), donc aucune perte fonctionnelle. **Redéploiement complet effectué le jour même** sur un nouveau projet propre (`newpad-bnw-v2`, réf. `vdbuvltfwulsxqjpwzhr`, région `eu-west-3` Paris — toujours "pas de réutilisation" comme demandé à l'origine), à partir des 14 fichiers de migration versionnés dans le dépôt : reconstruction à l'identique, vérifiée ci-dessous. **Incident totalement résolu** : `.env.production` mis à jour sur GitHub et "Confirm email" désactivé sur le nouveau projet (les deux confirmés faits par l'utilisateur) — le site en ligne est de nouveau pleinement opérationnel pour l'inscription/connexion.

Tous les fichiers sont dans `supabase/migrations/`, à exécuter dans l'ordre alphabétique de leur nom (0001 → 0007) :

- **0001 / 0001b / 0001c** — schéma complet : profils, registre de fonctionnalités générique, permissions par compte, paramètres économiques (globaux + exceptions par client), catégories de clientèle dynamiques (M:N), comptes + trésorerie banque, transactions (ledger unique), bénéficiaires, virements, lingots (registre + achats banque/marché), coffres-forts, prêts + échéancier, support (ticket + messages), consulting, adhésion + ouverture guichet, file clients, fraude (règles + alertes), rapports de caisse, masquage générique par interface, notifications, audit + journal de connexion, comptes IRS, CMS (`site_content`), documents.
- **0002 / 0002b / 0002c / 0002e** — toute la logique métier en fonctions `SECURITY DEFINER` : virements, lingots (banque + marché), coffres, prêts (demande → revue employé → décision admin exclusive → échéancier automatique → remboursement anticipé), adhésion, ouverture guichet, frais de gestion périodiques, intérêts d'épargne (désactivés par défaut), échéances de prêts automatiques, rapport de caisse quotidien + correction admin, support (fil de discussion), masquage, note de confiance, journal de connexion + détection fraude sur échecs répétés.
- **0003 / 0003b** — RLS activé sur **toutes** les tables + fonctions de lecture dédiées à l'IRS (`irs_stats`, `irs_list_clients`, `irs_list_accounts`, `irs_list_transactions`, `irs_list_gold_bars`) qui sont le **seul** chemin de lecture pour ce rôle (aucun grant direct sur les tables de base) + garde-fou anti-modification frauduleuse de son propre profil (rôle/statut/note de confiance).
- **0004** — données de référence (types de comptes, paramètres économiques par défaut, règles de fraude, registre de fonctionnalités, coffres et lingots de démonstration, catégories clientèle de départ, contenu CMS de la page d'accueil).
- **0005** — tâches planifiées `pg_cron` (frais de gestion, intérêts d'épargne, échéances de prêts, rapport de caisse quotidien).
- **0007** — `resolve_account_by_iban` : fonction `SECURITY DEFINER` dédiée permettant à un client de résoudre l'IBAN d'un tiers vers un compte destinataire (id + nom + type de compte, jamais le solde) au moment d'émettre un virement — nécessaire car RLS (`accounts_select`) ne permet à un client de lire que ses propres comptes.

**Testé** en local contre PostgreSQL 16 (voir `docs/DB_TESTING.md`) : inscription, adhésion, virement, achat de lingot, séparation employé/admin sur les prêts, garde-fou solde minimum → admin, isolation RLS entre clients, interdiction d'écriture directe hors fonctions. Un bug réel a été trouvé et corrigé pendant ces tests (voir le fichier ci-dessus).

**Déployé sur le vrai projet Supabase** (`newpad-bnw-v2`, réf. `vdbuvltfwulsxqjpwzhr`, région `eu-west-3` Paris, plan gratuit) : les 14 migrations (0001 → 0007) appliquées dans l'ordre. Les migrations de durcissement (**0006 / 0006b**) sont désormais intégrées dès le départ dans ce redéploiement :
- `search_path` fixé explicitement sur toutes les fonctions `SECURITY DEFINER` (évite tout risque de détournement par manipulation du chemin de recherche).
- Droit d'exécution des fonctions retiré aux rôles `public` et `anon` (visiteur non connecté) : aucune fonction métier n'a besoin d'être appelable sans session — seul `authenticated` (et `service_role`) peut les exécuter ; le contenu public passe uniquement par la lecture directe de `site_content`, protégée par sa propre policy RLS.
- Vérifié après coup : 35/35 tables avec RLS actif, données de référence en place (23 fonctionnalités au registre, 19 paramètres économiques, 4 types de comptes, 1 compte trésorerie à 250 M$, 6 lingots, 5 coffres, 4 catégories clientèle, 15 blocs de contenu public), 4 tâches `pg_cron` planifiées. Avis de sécurité Supabase : uniquement des avis informatifs attendus par construction ("les utilisateurs connectés peuvent exécuter telle fonction" — c'est le principe même de l'architecture : ces fonctions sont l'unique porte d'entrée pour tout mouvement de fonds, avec leurs propres vérifications de rôle internes). Aucune erreur de sécurité réelle.

**Edge Function `create-account` déployée** sur le projet (`vdbuvltfwulsxqjpwzhr`, fonction `create-account`, `verify_jwt: false` — l'autorisation est vérifiée manuellement dans le code via le JWT transmis, `verify_jwt: false` étant nécessaire pour que le préflight CORS `OPTIONS` (sans JWT) passe la porte d'entrée Supabase avant même d'atteindre le code de la fonction) : seule opération de tout le backend qui utilise la clé `service_role`, jamais exposée côté client — elle tourne côté serveur Supabase avec ses propres variables d'environnement (`SUPABASE_SERVICE_ROLE_KEY`). Elle crée un utilisateur Supabase Auth (`auth.admin.createUser`), ce que la base ne peut pas faire elle-même (aucune fonction SQL ne peut écrire dans `auth.users`) ; le trigger `handle_new_auth_user` existant crée ensuite automatiquement la ligne `profiles` avec le rôle demandé. Vérifie elle-même les permissions : un employé peut créer un compte `client` (cas guichet), seul un admin peut créer `employee`/`admin`/`irs`. Toute création est journalisée dans `audit_log`. Débloque le point 5 de §5.
> ⚠️ **Bug trouvé et corrigé lors du premier test réel (v2)** : la v1 ne gérait pas les en-têtes CORS ni le préflight `OPTIONS`, ce qui bloquait silencieusement tout appel navigateur depuis le site GitHub Pages ("Failed to send a request to the Edge Function", aucun détail exploitable côté client). Corrigé en ajoutant `Access-Control-Allow-Origin`/`Allow-Headers`/`Allow-Methods` sur toutes les réponses et une gestion explicite d'`OPTIONS`. Redéployé (v2) — aucune modification frontend nécessaire, juste redéployer la fonction. À reconfirmer par l'utilisateur.

**Pas encore fait** : script de génération des comptes de démonstration (nécessite l'API Admin Supabase, prévu Phase 8).

## 4. Frontend — ce qui est fait

- Scaffold Vite + JS vanilla (pas de framework lourd), cible `es2017` + polyfills legacy (`@vitejs/plugin-legacy`) pour le navigateur intégré FiveM, build statique testé avec succès.
- Design system complet dans `src/styles/tokens.css` (couleurs, typographies, rayons, ombres — toute la charte y est centralisée).
- Logo recréé en SVG vectoriel propre (`src/assets/logo.svg`), fidèle au triangle "A" doré existant.
- Routeur hash minimal (`src/lib/router.js`) — robuste sur GitHub Pages sans configuration serveur.
- Client Supabase + conversion identifiant → e-mail synthétique (`src/lib/supabaseClient.js`).
- Écrans fonctionnels : connexion, inscription prospect, page d'accueil publique (contenu piloté par `site_content` avec repli si hors-ligne).
- Coquille commune (sidebar catégorisée repliable + topbar + notifications temps réel via Supabase Realtime) partagée par les interfaces internes — `src/lib/shell.js` + `src/lib/notifications.js` + `src/lib/features.js` (résolution générique `feature_registry`/`permission_grants`, réutilisée par Client et Employé).
- Captures d'écran validées visuellement (accueil public, connexion) — rendu conforme à l'identité demandée.
- **Interface Client (phase 3, 11 écrans)** : tableau de bord, comptes (détail + historique), virements (interne / bénéficiaire / IBAN externe avec résolution en direct via `resolve_account_by_iban`), bénéficiaires, lingots d'or (achat banque + marché de revente), coffres-forts, prêts professionnels (demande, échéancier, remboursement anticipé), consulting premium, documents, support (tickets en fil de discussion), paramètres. Ajoute aussi l'écran et la route manquante pour le rôle `prospect` (demande d'adhésion), qui comblait un trou de routing (boucle de redirection après inscription).
- **Interface Employé (phase 4, 16 écrans)** : tableau de bord (compteurs des files d'attente), recherche clients (fiche complète : comptes, catégories dynamiques, prêts), demandes d'adhésion (prise en charge + décision), ouverture de compte au guichet (crée maintenant directement le compte du client via l'Edge Function `create-account`, en plus de l'option de relier une demande à un profil déjà auto-inscrit), file d'attente, traitement des virements/lingots (banque + marché)/coffres-forts, réception des prêts (décision finale réservée à l'admin), consulting, rapport de caisse (lecture seule), alertes fraude (signalement manuel + traitement), support (vue personnel sur tous les tickets), journal d'activité, paramètres.
- **Interface Admin (phase 5, 23 écrans)** : reprend tous les écrans Employé (mêmes fonctionnalités, coquille et routes `/admin/*` dédiées) et ajoute : décision finale des prêts (`admin_decide_loan`, sur `/admin/loans`), registre complet des lingots + frappe de nouveaux lingots (`mint_gold_bar`/`admin_update_gold_bar`, sur `/admin/gold`), correction manuelle du rapport de caisse (`admin_adjust_cashier_report`, sur `/admin/cashier`), gestion des employés/rôles (`/admin/staff` — changement de rôle/titre/statut d'un profil existant via `profiles_admin_all`), permissions génériques (`/admin/permissions` — registre de fonctionnalités + exceptions par compte), comptes IRS (`/admin/irs-accounts` — octroi/révocation), masquage compte-ou-transaction par interface (`/admin/visibility`), pilotage économique complet + exceptions par client (`/admin/economic-settings`), CMS du site (`/admin/cms` — édition JSON des sections lues par la page d'accueil publique), configuration système (`/admin/system` — `maintenance_mode`/`announcement_banner` et autres réglages `category='system'`). Dans le détail client (`/admin/clients`), ajoute aussi le gel/suspension du profil, le gel/fermeture de compte et l'édition des seuils individuels (solde minimum, minimum de virement, note de confiance).
- `/admin/staff` propose maintenant **deux façons** de donner un accès personnel : créer directement un compte employé/admin/IRS (identifiant + mot de passe, via l'Edge Function `create-account`), ou promouvoir un profil déjà inscrit (prospect/client) en changeant son rôle. Les deux mènent au même résultat, la création directe est la voie recommandée pour un nouveau membre du personnel.
- **Interface IRS (phase 6, 6 écrans)** : tableau de bord (statistiques agrégées via `irs_stats`), clients, comptes, transactions, lingots d'or, paramètres (profil/mot de passe). Strictement lecture seule — le rôle IRS n'a **aucune** policy RLS sur les tables de base (deny by default) : toutes les lectures passent exclusivement par les fonctions `SECURITY DEFINER` dédiées `irs_stats`/`irs_list_clients`/`irs_list_accounts`/`irs_list_transactions`/`irs_list_gold_bars` (0003_policies.sql), qui appliquent elles-mêmes le masquage par interface (`visibility_masks` géré par l'admin) et ne contiennent aucune capacité d'écriture. La coquille IRS affiche un bandeau permanent rappelant l'accès lecture seule (nouvelle option `banner` ajoutée à `src/lib/shell.js`, réutilisable par les autres rôles si besoin). La trésorerie de la banque est explicitement exclue du registre des comptes IRS (`irs_list_accounts` filtre `is_bank_treasury = false`).
- Toutes les écritures qui touchent à l'argent passent exclusivement par les fonctions `SECURITY DEFINER` existantes (aucun insert/update direct de solde, conforme au principe central du cahier des charges) ; les tables non-financières de configuration/administration (file d'attente, alertes fraude, catégories clientèle, registre de fonctionnalités, permissions, paramètres économiques, comptes IRS, contenu du site) acceptent une écriture directe côté staff/admin, explicitement autorisée par les policies RLS `is_staff()`/`is_admin()`.
- Build Vite vérifié sans erreur après chaque phase + test en navigateur headless (Playwright) sans erreur JavaScript sur les routes publiques.

## 5bis. Lot de fonctionnalités additionnel (17/08/2026)

Suite à une demande explicite et détaillée de l'utilisateur listant 10 améliorations, traitées dans cette mise à jour :

- **Identifiant Discord** : nouvelle colonne `profiles.discord_id` (migration `0008_discord_and_audit`), renseignable à l'inscription prospect (`/signup`), à la création directe d'un compte (Edge Function `create-account`, désormais en v3 avec le paramètre `discordId`) et modifiable pour un profil existant depuis `/admin/staff`. Sert de base à la réinitialisation de mot de passe via Discord (point bloquant, voir §5).
- **Cours de l'or à l'once** : affiché sur `/client/gold` (conversion `prix/gramme × 31,1034768`) et sur `/admin/economic-settings` en regard du paramètre `gold_price_per_gram`.
- **Lingots en circulation / possédés** : `/admin/gold` et le tableau de bord admin affichent désormais le nombre de lingots en circulation (statut `sold`) et leur poids total ; `/client/gold` affiche le poids et la valeur totale possédée par le client ; `/admin/clients` affiche les lingots possédés par le client sélectionné (nouvelle fonction `getClientGoldBars`).
- **Catégories clientèle** : `/employee/clients` et `/admin/clients` permettent désormais de filtrer la recherche par catégorie (nouvelle fonction `getClientIdsInCategory`) et de créer une nouvelle catégorie directement depuis l'écran (nouvelle fonction `createClientCategory`, autorisée par la policy RLS `client_categories_staff_write` déjà en place).
- **Adaptation mobile** : règles CSS responsives ajoutées dans `src/styles/base.css` (`@media max-width: 720px`) — toutes les grilles à colonnes multiples repassent en une seule colonne, les tableaux larges deviennent défilables horizontalement au lieu d'écraser le texte, padding/typographie réduits, `font-size: 16px` sur les champs de saisie pour éviter le zoom automatique iOS.
- **Journal d'activité enrichi** : la migration `0008` ajoute des appels `log_audit(...)` à toutes les fonctions de décision métier qui n'en avaient pas encore (virements, lingots, coffres, prêts, adhésion, support, consulting), avec le nom du client/montant/contexte dans le détail. `/admin/audit` et `/employee/audit` affichent maintenant le rôle de l'auteur et une traduction lisible de l'action + du détail (nouvelles fonctions partagées `auditActionLabel`/`auditDetailsText` dans `src/lib/format.js`) au lieu du nom brut de la fonction SQL et d'un JSON illisible.
- **Suivi de virement côté client** : `/client/transfers` affiche désormais des statuts spécifiques au virement plutôt que les libellés génériques partagés — "Demande envoyée" (pending) → "En cours de traitement" (processing, dès qu'un employé/admin prend en charge) → "Virement effectué" (validated) → "Refusé" (rejected). Bug corrigé au passage : les virements **internes** (entre comptes du même client) ne notifiaient jamais le personnel (`submit_transfer` ne le faisait que pour les virements externes) — ils pouvaient rester invisibles indéfiniment ; corrigé dans la migration `0008`.
- **Non fait dans ce lot — bloqué sur des éléments externes que je ne peux pas créer moi-même** : réinitialisation de mot de passe par Discord (nécessite un bot Discord) et CAPTCHA anti-spam/DDoS (nécessite une clé Cloudflare Turnstile). Détail et marche à suivre en §5, points 6 et 7.

## 5bis-ter. CAPTCHA, réinitialisation Discord, liens Discord, messagerie, trésorerie (18/08/2026)

Quatre demandes traitées à la suite, dans cet ordre :

- **CAPTCHA Cloudflare Turnstile** : clé de site fournie par l'utilisateur (`0x4AAAAAAES6BOvwjwDOOqdz`), intégrée sur `/login` et `/signup` (`src/lib/turnstile.js`, chargement dynamique du script Turnstile, jeton transmis à `signInWithUsername`/`signUpProspect` puis à Supabase Auth via `options.captchaToken`). Ancien point bloquant §5.7 levé côté frontend — reste à l'utilisateur d'activer "Bot and Abuse Protection" avec la Secret Key côté tableau de bord Supabase (Authentication > Attack Protection), réglage que je ne peux pas activer moi-même.
- **Réinitialisation de mot de passe par Discord** : nouvelle table `password_reset_codes` (migration `0009`) + deux Edge Functions dédiées `verify_jwt: false` (`request-password-reset`, `confirm-password-reset`) + assistant en 2 étapes `/forgot-password`. Code à 6 chiffres, haché SHA-256, expire en 15 min, 5 tentatives max, cooldown 60s, message générique anti-énumération. Le bot Discord envoie le code par message privé via l'API REST Discord (`DISCORD_BOT_TOKEN`, configuré par l'utilisateur dans Supabase Edge Functions > Secrets — je n'ai jamais reçu ni demandé ce jeton). Ancien point bloquant §5.6 levé.
- **Liens Discord sur tout le site** : `https://discord.gg/ptJ9ksjpgs` ajouté dans la catégorie Support/Services des 4 interfaces internes (Client/Employé/Admin/IRS — IRS n'avait auparavant aucune catégorie Support), en haut à droite de la barre de navigation publique (à côté de "Se connecter"), et sous le formulaire de connexion ("Rejoindre le Discord").
- **Messagerie inter-rôles** (migration `0010`) : nouvelles tables `message_threads`/`thread_messages`, RLS strictement limitée aux deux participants (aucune policy d'écriture — tout passe par les fonctions `SECURITY DEFINER` `create_message_thread`/`send_thread_message`/`mark_thread_read`/`close_message_thread`/`list_my_message_threads`/`list_messageable_contacts`). N'importe quelle paire de rôles peut échanger (Client↔Employé, Client↔Admin, Client↔IRS, Employé↔Admin, Employé↔IRS, Admin↔IRS, etc.) **sauf Client↔Client**, refusé côté serveur avec un message explicite. Nouvel écran "Messagerie" partagé (`src/pages/shared/messagesScreen.js`) branché dans les 4 interfaces (`/client/messages`, `/employee/messages`, `/admin/messages`, `/irs/messages`), avec composition d'un nouveau message (choix du destinataire parmi les contacts autorisés), fil de discussion, clôture, notifications en temps réel (Realtime activé sur les deux nouvelles tables, et sur `notifications` qui n'y était pas encore malgré l'abonnement déjà en place côté frontend). Le bandeau "lecture seule" de l'interface IRS a été reformulé pour préciser que la messagerie fait exception.
- **Trésorerie — fonds propres vs actif en gestion** : `decide_membership_request()` et `finalize_manual_account_opening()` créditaient auparavant le compte du nouveau client sans jamais débiter la banque (l'argent apparaissait depuis rien). Les deux fonctions débitent désormais le compte de trésorerie (`bank_treasury_account_id()`) du même montant que le dépôt initial, et la transaction `cash_deposit` correspondante a maintenant un `from_account_id` renseigné. Nouvelle fonction `admin_treasury_stats()` (admin uniquement) exposant trois montants distincts, affichés en cartes sur le tableau de bord admin : **fonds propres** (solde réel du compte de trésorerie — l'argent qui appartient à la banque), **actif en gestion** (somme des soldes de tous les comptes clients actifs — l'argent des clients géré par la banque), **solde total** (somme des deux).
- **Correction des liens de notification cassés** : plusieurs fonctions notifiaient avec un chemin qui ne correspondait à aucune route réelle (segments `/operations/...` inexistants, `/admin/clients/membership`, `/admin/clients/openings`, liens `null` sur les notifications prospect). Corrigé dans `submit_transfer`, `decide_transfer`, `submit_gold_bank_purchase`, `submit_market_purchase`, `decide_gold_bank_purchase`, `decide_market_purchase`, `submit_safe_request`, `confirm_safe_rental`, `submit_loan_request`, `decide_membership_request`, `finalize_manual_account_opening`, `claim_membership_request` — un clic sur n'importe quelle notification amène désormais bien à l'écran attendu. Corrigé aussi côté frontend (`src/lib/shell.js`) : `notify_all_staff()` envoie le même lien `/employee/...` aux employés et aux admins (sauf notifications réservées à l'admin) ; un admin qui cliquait dessus se faisait rediriger vers son propre tableau de bord par la garde de rôle (`guardedRoleRender`) au lieu de l'écran visé — le lien est maintenant adapté au rôle réel avant la navigation.
- **Navigation de la page d'accueil publique** : les liens d'ancre "Services" et "Actualités" (`#services`, `#news`) étaient interceptés à tort par le routeur global basé sur le hash (qui traite tout changement de hash comme une tentative de route et renvoyait vers l'accueil, rechargement complet, au lieu de défiler jusqu'à la section). Corrigé avec un défilement en douceur géré en JavaScript (`scrollIntoView`), sans passer par le routeur.

## 5. Ce qui bloque une mise en ligne réelle

1. ~~Connexion Supabase~~ ✅ Fait : projet recréé (`newpad-bnw-v2`, réf. `vdbuvltfwulsxqjpwzhr`) suite à l'incident de suppression du 15/08, migrations redéployées, `.env.local`/`.env.production` (local et sur GitHub) mis à jour avec l'URL et la clé anon du nouveau projet.
2. ~~Désactiver "Confirm email"~~ ✅ Fait sur le nouveau projet (`newpad-bnw-v2`) — confirmé par l'utilisateur.
3. ~~Dépôt GitHub~~ ✅ Fait, avec une méthode différente de celle prévue : l'environnement cloud bloque tout push git/API sortant vers GitHub (proxy interne : "repository not in this session's authorized repository set", aucun outil disponible pour lever cette restriction). Contournement établi et répété depuis : le dossier `src/` mis à jour est livré sous forme de zip à l'utilisateur, qui le glisse-dépose à la racine du dépôt via "Add file → Upload files" sur `github.com/bouzoumacabre/newpad" (GitHub reconnaît les chemins existants et met à jour/ajoute les fichiers automatiquement).
4. ~~Déploiement GitHub Pages~~ ✅ **Fait et vérifié en ligne** : `https://bouzoumacabre.github.io/newpad/`. Workflow `.github/workflows/deploy.yml` (build Vite + `actions/deploy-pages`) opérationnel, se redéclenche à chaque push sur `main`. Un blocage initial ("Get Pages site failed — Not Found") a été résolu en re-sélectionnant la source "GitHub Actions" dans Settings > Pages (bascule sur "Deploy from a branch" puis retour, pour forcer l'activation côté GitHub) avant de relancer le job.
5. ~~Edge Function de création de comptes~~ ✅ **Fait** : `create-account` déployée sur le projet Supabase, utilisée par l'ouverture de compte au guichet (Employé/Admin) et par `/admin/staff` pour créer directement un employé/admin/IRS. Voir §3 pour le détail technique. Non testée en conditions réelles depuis ce sandbox (réseau sortant vers Supabase bloqué ici) — à valider par l'utilisateur en jeu.
6. ~~Réinitialisation de mot de passe via Discord~~ ✅ **Fait côté code** (voir §5bis-ter) : code à usage unique envoyé en message privé par le bot. Reste à la charge de l'utilisateur (je ne peux pas le faire moi-même) : créer le bot Discord, l'inviter sur le serveur Hurricane FA avec la permission d'envoyer des messages privés, et renseigner `DISCORD_BOT_TOKEN` dans Supabase Edge Functions > Secrets.
7. ~~CAPTCHA anti-spam/DDoS~~ ✅ **Fait côté code** (voir §5bis-ter) : widget Turnstile intégré sur `/login` et `/signup` avec la Site Key fournie. Reste à la charge de l'utilisateur : activer « Bot and Abuse Protection » avec la Secret Key dans Authentication > Attack Protection sur le tableau de bord Supabase (réglage que je ne peux pas activer moi-même).

**Important pour la suite** : je n'ai toujours pas d'accès en écriture (push) vers GitHub depuis cet environnement — chaque mise à jour future du frontend devra repasser par la même méthode manuelle (zip + upload web par l'utilisateur), sauf si un moyen d'autoriser le dépôt pour la session apparaît côté compte Claude/Cowork.

## 6. Prochaines étapes

1. ~~Connecter Supabase → créer le projet et exécuter les migrations.~~ ✅ Fait.
2. ~~Dépôt GitHub + déploiement GitHub Pages.~~ ✅ Fait — site en ligne à `https://bouzoumacabre.github.io/newpad/`.
3. ~~Construire l'interface Client écran par écran (phase 3).~~ ✅ Fait.
4. ~~Construire l'interface Employé (phase 4).~~ ✅ Fait.
5. ~~Construire l'interface Admin (phase 5).~~ ✅ Fait — 23 écrans (voir §4).
6. ~~Construire l'interface IRS (phase 6).~~ ✅ Fait — 6 écrans, lecture seule stricte via les fonctions `irs_*` (voir §4).
7. ~~Edge Function de création de comptes.~~ ✅ Fait — `create-account` déployée, intégrée au guichet et à `/admin/staff` (voir §3 et §5, point 5).
8. Notifications temps réel : le mécanisme (`src/lib/notifications.js`, Supabase Realtime) est déjà branché dans la coquille commune et fonctionne pour les 4 rôles internes (Client, Employé, Admin, IRS). Reste à vérifier ce mécanisme en conditions réelles en jeu (phase 7).
9. Données de démo réalistes, tests de bout en bout — y compris la nouvelle création de compte directe (Edge Function) — en conditions réelles (phase 8).
10. ~~Lot de fonctionnalités additionnel (Discord ID, cours de l'or à l'once, lingots en circulation/possédés, catégories clientèle filtrables, adaptation mobile, journal d'activité enrichi, suivi de virement)~~ ✅ Fait — voir §5bis.
11. ~~CAPTCHA Turnstile + réinitialisation de mot de passe Discord + liens Discord sur tout le site + messagerie inter-rôles + trésorerie fonds propres/actif en gestion + correction des liens de notification + correction navigation d'ancre de l'accueil~~ ✅ Fait côté code — voir §5bis-ter pour le détail. Reste à la charge de l'utilisateur : créer/inviter le bot Discord et renseigner `DISCORD_BOT_TOKEN`, activer la Secret Key Turnstile côté Supabase.
12. Prochaine étape immédiate : vérification en jeu de l'ensemble du lot livré aujourd'hui (envoyer un message d'un rôle à l'autre pour vérifier la messagerie, valider une nouvelle demande d'adhésion et vérifier au tableau de bord admin que fonds propres/actif en gestion évoluent comme attendu, cliquer sur plusieurs notifications pour vérifier les redirections, tester les liens "Services"/"Actualités" de l'accueil). Une fois le bot Discord et la Secret Key Turnstile en place côté utilisateur, confirmer que la réinitialisation de mot de passe et le CAPTCHA fonctionnent de bout en bout en jeu.
