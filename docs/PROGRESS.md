# Newpad — Inventaire, phases, état d'avancement

Dernière mise à jour : 19 août 2026 (fusion inscription + demande d'adhésion en une seule étape — voir §5quinquies ; lot précédent du même jour : retrait du CAPTCHA Cloudflare Turnstile, remplacé par un honeypot — voir §5quater ; lot du 18 août : réinitialisation de mot de passe par Discord, liens Discord sur tout le site, messagerie inter-rôles, trésorerie fonds propres/actif en gestion, correction des liens de notification, correction de la navigation d'ancre de l'accueil — voir §5ter).

## 5quinquies. Fusion inscription + demande d'adhésion en une seule étape (19/08/2026)

Retour utilisateur : le parcours en deux étapes distinctes (1. `/signup` crée juste un identifiant/mot de passe puis renvoie vers `/login` ; 2. une fois reconnecté, l'écran `/prospect` propose de soumettre la demande d'adhésion) existait depuis la toute première version du site (Phase 1/3, confirmé par l'historique git — ce n'est pas un changement introduit par un lot précédent) mais ne correspondait pas au fonctionnement voulu : une seule étape, avec le message "Demande en cours d'examen" affiché immédiatement après l'inscription.

`/signup` (`src/pages/auth/signup.js`) intègre désormais directement les champs de la demande d'adhésion (type de compte, dépôt initial, motivation) en plus des champs de création de compte. À la soumission : création du compte (`signUpProspect`, session ouverte immédiatement car la confirmation par e-mail est désactivée côté Supabase) puis, dans la foulée, envoi de la demande d'adhésion (`submitMembershipRequest`) — puis redirection vers `/prospect`, qui affiche directement l'écran d'attente "Demande en cours d'examen" (aucun passage par `/login` entre les deux). Si la création du compte réussit mais l'envoi de la demande échoue (cas limite), l'utilisateur est renvoyé vers `/prospect` où le formulaire de demande reste disponible pour réessayer manuellement — le compte n'est jamais perdu.

L'écran `/prospect` et son formulaire (`src/pages/client/membership-request.js`) restent inchangés et toujours utiles : ils servent de filet de sécurité pour les anciens comptes prospects créés avant ce correctif, et pour la resoumission d'une demande refusée (`existing.status === 'rejected'`).

## 5quater. Retrait du CAPTCHA Cloudflare Turnstile — incompatibilité CEF (19/08/2026)

Incident en production : une fois « Bot and Abuse Protection » activé côté Supabase (Authentication > Attack Protection), **toutes les connexions et inscriptions depuis le navigateur intégré de FiveM (CEF) ont commencé à échouer** avec l'erreur Cloudflare `600010` ("bot behavior detected"), alors que ça fonctionnait normalement depuis un navigateur classique (Chrome/Edge). Diagnostic confirmé via les logs Supabase (`captcha protection: request disallowed`) puis via un ticket officiel du dépôt CEF ([chromiumembedded/cef#3547](https://github.com/chromiumembedded/cef/issues/3547), fermé "not planned") : le moteur CEF échoue structurellement aux vérifications d'intégrité de navigateur de Cloudflare — ce n'est pas un problème de configuration côté Newpad, et ça ne peut pas être corrigé en ajustant le widget.

Turnstile a donc été entièrement retiré (`src/lib/turnstile.js` supprimé, widgets retirés de `/login` et `/signup`, clé `VITE_TURNSTILE_SITE_KEY` inutilisée) et remplacé par un **honeypot** : un champ de formulaire invisible (`website`, positionné hors écran, jamais rempli par un humain) transmis dans les métadonnées d'inscription et vérifié côté serveur dans le trigger `handle_new_auth_user()` (migration `0011_honeypot_signup_guard`) — si le champ est renseigné, la création du compte est rejetée avant même l'insertion du profil. Fonctionne identiquement en jeu et hors jeu, sans dépendance à un service externe. La protection anti-abus « Bot and Abuse Protection » a été désactivée côté tableau de bord Supabase (elle est maintenant inutile et bloquerait à nouveau les joueurs en jeu si réactivée).

## 1. Inventaire (résumé de cadrage)

- **4 rôles + accueil public** : Client, Employé, Admin (= employé + pouvoirs élargis), IRS (staff Hurricane FA, lecture seule garantie en base). Une seule base de données, un seul déploiement.
- **Authentification** : identifiant + mot de passe, e-mail synthétique `identifiant@newpad.local` en interne (jamais montré). Prospect → inscription libre → demande d'adhésion → activation automatique du compte client à la validation employé/admin. Employé/admin/IRS créés manuellement.
- **Client** : dashboard, infos ville, comptes multiples (IBAN, solde), virements (≥ 100 000 $ sauf entre ses propres comptes), transactions, bénéficiaires, lingots d'or (achat banque + marché de revente), coffres-forts, prêts professionnels, consulting premium, documents, support (tickets = conversation), paramètres.
- **Employé** : dashboard, recherche/registre clients (catégories dynamiques), ouverture de compte guichet, demandes d'adhésion, file clients, traitement virements/prêts (réception seulement)/coffres/lingots, rapports de caisse (lecture), alertes fraude, support, journal d'activité.
- **Admin** : tout ce que fait l'employé (+ correction manuelle du rapport de caisse) + validation finale des prêts + frappe/édition directe du registre de lingots + autorisation des opérations sous le solde minimum + gestion des employés/comptes clients/permissions génériques par compte/comptes IRS/masquage compte-ou-transaction par interface + pilotage économique complet (tous les seuils, avec exceptions par client) + contenu de tout le site (CMS) + audit & sécurité + rapports & statistiques + configuration système (maintenance, bannière, activation des fonctionnalités).
- **IRS** : interface dédiée à part entière (comme les 3 autres), lecture seule sur les transactions/comptes/stats agrégées, restriction garantie par RLS (aucun grant d'écriture, aucun accès direct aux tables de base — uniquement via des fonctions `irs_*` dédiées), soumise au masquage par interface.
- **Processus métier communs** : demande → notification → "en cours de traitement" → décision. Aucune saisie manuelle de solde nulle part : chaque mouvement de fonds découle exclusivement de la validation d'une action prévue par le système, via des fonctions Postgres atomiques.
- **Pilotage économique 100% dynamique** : solde minimum (global + exception par client), montant minimum de virement, fourchette de prix marketplace lingots, taux prêt/épargne/pénalité de retard, cours de l'or, frais et commissions — tout en base, rien codé en dur.
- **Contraintes techniques** : Supabase (Postgres + Auth + Realtime + Storage + pg_cron), compatibilité navigateur intégré FiveM, déploiement GitHub Pages, RLS pour toutes les restrictions d'accès (y compris IRS et masquage).

## 2. Découpage en phases

| Phase | Contenu | Statut |
|---|---|---|
| 0 | Cadrage, inventaire, décisions infra (Supabase connecté, format multi-fichiers) | ✅ Fait |
| 1 | Fondations : schéma DB complet, RLS, fonctions métier, socle frontend (design system, routing, auth) | ✅ Déployé sur le vrai projet Supabase (`newpad-bnw-v2`, réf. `vdbuvltfwulsxqjpwzhr`, région Paris — voir incident et redéploiement §3). Socle frontend fonctionnel et reconnecté (`.env.production` mis à jour sur GitHub, "Confirm email" désactivé sur le nouveau projet — site de nouveau opérationnel pour l'auth) |
| 2 | Page d'accueil publique | ✅ Écran construit, relié au vrai projet Supabase (`site_content`, piloté depuis l'admin une fois Phase 5 construite ; fallback conservé si hors-ligne) |
| 3 | Interface Client (tous les écrans) | ✅ Construite — 11 écrans (voir §4). Livrée via zip pour upload manuel GitHub. |
| 4 | Interface Employé | ✅ Construite — 16 écrans (voir §4). Ouverture de compte au guichet désormais pleinement fonctionnelle (Edge Function `create-account` déployée, voir §5). Livrée via zip pour upload manuel GitHub. |
| 5 | Interface Admin | ✅ Construite — 23 écrans (voir §4). Livrée via zip pour upload manuel GitHub. |
| 6 | Interface IRS | ✅ Construite — 6 écrans, strictement lecture seule (voir §4). Livrée via zip pour upload manuel GitHub. |
| 7 | Automatisations & temps réel (notifications, Realtime, tâches planifiées) | 🟡 Fonctions et jobs `pg_cron` écrits côté DB (0002/0005) ; notifications realtime branchées côté frontend pour les 4 rôles (`src/lib/notifications.js`) et déjà vérifiées visuellement pour Client/Employé ; reste à vérifier en conditions réelles pour Admin/IRS une fois le site utilisé en jeu |
| 8 | Données de démo, tests, déploiement GitHub Pages | 🟡 Déploiement GitHub Pages fait et vérifié en ligne (`bouzoumacabre.github.io/newpad`) ; données de démo et tests de bout en bout restent à faire |

## 3. Base de données — ce qui est fait

> ⚠️ **Incident du 15/08/2026** : le projet Supabase d'origine (`newpad-bnw`, réf. `hvtonptowwriprbmmzmw`) a été supprimé par erreur (nettoyage voulu d'un autre projet, obsolète et sans rapport — `NEWMAN BANK` — mais suppression du mauvais projet). Aucune donnée réelle de jeu n'était encore en base (uniquement les données de référence de la migration 0004), donc aucune perte fonctionnelle. **Redéploiement complet effectué le jour même** sur un nouveau projet propre (`newpad-bnw-v2`, réf. `vdbuvltfwulsxqjpwzhr`, région `eu-west-3` Paris — toujours "pas de réutilisation" comme demandé à l'origine), à partir des 14 fichiers de migration versionnés dans le dépôt : reconstruction à l'identique, vérifiée ci-dessous. **Incident totalement résolu** : `.env.production` mis à jour sur GitHub et "Confirm email" désactivé sur le nouveau projet (les deux confirmés faits par l'utilisateur) — le site en ligne est de nouveau pleinement opérationnel pour l'inscription/connexion.

Tous les fichiers sont dans `supabase/migrations/`, à exécuter dans l'ordre alphabétique de leur nom (0001 → 0007) :

- **0001 / 0001b / 0001c** — schéma complet : profils, registre de fonctionnalités générique, permissions par compte, paramètres économiques (globaux + exceptions par client), catégories de clientèle dynamiques (M:N), comptes + trésorerie banque, transactions (ledger unique), bénéficiaires, virements, lingots (registre + achats banque/marché), coffres-forts, prêts + échéancier, support (ticket + messages), consulting, adhésion + ouverture guichet, file clients, fraude (règles + alertes), rapports de caisse, masquage générique par interface, notifications, audit + journal de connexion, comptes IRS, CMS (`site_content`), documents.
- **0002 / 0002b / 0002c / 0002e** — toute la logique métier en fonctions `SECURITY DEFINER` : virements, lingots (banque + marché), coffres, prêts (demande → revue employé → décision admin exclusive → échéancier automatique → remboursement anticipé), adhésion, ouverture guichet, frais de gestion périodiques, intérêts d'épargne (désactivés par défaut), échéances de prêts automatiques, rapport de caisse quotidien + correction admin, support (fil de discussion), masquage, note de confiance, journal de connexion + détection fraude sur échecs répétés.
- **0003 / 0003b** — RLS activé sur **toutes** les tables + fonctions de lecture dédiées à l'IRS (`irs_stats`, `irs_list_clients`, `irs_list_accounts`, `irs_list_transactions`, `irs_list_gold_bars`) qui sont le **seul** chemin de lecture pour ce rôle (aucun grant direct sur les tables de base) + garde-fou anti-modification frauduleuse de son propre profil (rôle/statut/note de confiance).
- **0004** — données de référence (types de comptes, paramètres économiques par défaut, règles de fraude, registre de fonctionnalités, coffres et lingots de démonstration, catégories clientèle de départ, contenu CMS de la page d'accueil).
- **0005** — tâches planifiées `pg_cron` (frais de gestion, intérêts d'épargne, échéances de prêts, rapport de caisse quotidien).
- **0007** — `resolve_account_by_iban` : fonction `SECURITY DEFINER` dédiée permettant à un client de résoudre l'IBAN d'un tiers vers un compte destinataire (id + nom + type de compte, jamais le solde) au moment d'émettre un virement — nécessaire car RLS (`accounts_select`) ne permet à un client de lire que ses propres comptes.

**Testé** en local contre PostgreSQL 16 (voir `docs/DB_TESTING.md`) : inscription, adhésion, virement, achat de lingot, séparation employé/admin sur les prêts, garde-fou solde minimum → admin, isolation RLS entre clients, interdiction d'écriture directe hors fonctions. Un bug réel a été trouvé et corrigé pendant ces tests (voir le fichier ci-dessus).

**Déployé sur le vrai projet Supabase** (`newpad-bnw-v2`, réf. `vdbuvltfwulsxqjpwzhr`, région `eu-west-3` Paris, plan gratuit) : les 14 migrations (0001 → 0007) appliquées dans l'ordre. Les migrations de durcissement (**0006 / 0006b**) sont désormais intégrées dès le départ dans ce redéploiement :
- `search_path` fixé explicitement sur toutes les fonctions `SECURITY DEFINER` (évite tout risque de détournement par manipulation du chemin de recherche).
- Droit d'exécution des fonctions retiré aux rôles `public` et `anon` (visiteur non connecté) : aucune fonction métier n'a besoin d'être appelable sans session — seul `authenticated` (et `service_role`) peut les exécuter ; le contenu public passe uniquement par la lecture directe de `site_content`, protégée par sa propre policy RLS.
- Vérifié après coup : 35/35 tables avec RLS actif, données de référence en place (23 fonctionnalités au registre, 19 paramètres économiques, 4 types de comptes, 1 compte trésorerie à 250 M$, 6 lingots, 5 coffres, 4 catégories clientèle, 15 blocs de contenu public), 4 tâches `pg_cron` planifiées. Avis de sécurité Supabase : uniquement des avis informatifs attendus par construction ("les utilisateurs connectés peuvent exécuter telle fonction" — c'est le principe même de l'architecture : ces fonctions sont l'unique porte d'entrée pour tout mouvement de fonds, avec leurs propres vérifications de rôle internes). Aucune erreur de sécurité réelle.

**Edge Function `create-account` déployée** sur le projet (`vdbuvltfwulsxqjpwzhr`, fonction `create-account`, `verify_jwt: false` — l'autorisation est vérifiée manuellement dans le code via le JWT transmis, `verify_jwt: false` étant nécessaire pour que le préflight CORS `OPTIONS` (sans JWT) passe la porte d'entrée Supabase avant même d'atteindre le code de la fonction) : seule opération de tout le backend qui utilise la clé `service_role`, jamais exposée côté client — elle tourne côté serveur Supabase avec ses propres variables d'environnement (`SUPABASE_SERVICE_ROLE_KEY`). Elle crée un utilisateur Supabase Auth (`auth.admin.createUser`), ce que la base ne peut pas faire elle-même (aucune fonction SQL ne peut écrire dans `auth.users`) ; le trigger `handle_new_auth_user` existant crée ensuite automatiquement la ligne `profiles` avec le rôle demandé. Vérifie elle-même les permissions : un employé peut créer un compte `client` (cas guichet), seul un admin peut créer `employee`/`admin`/`irs`. Toute création est journalisée dans `audit_log`. Débloque le point 5 de §5.
> ⚠️ **Bug trouvé et corrigé lors du premier test réel (v2)** : la v1 ne gérait pas les en-têtes CORS ni le préflight `OPTIONS`, ce qui bloquait silencieusement tout appel navigateur depuis le site GitHub Pages ("Failed to send a request to the Edge Function", aucun détail exploitable côté client). Corrigé en ajoutant `Access-Control-Allow-Origin`/`Allow-Headers`/`Allow-Methods` sur toutes les réponses et une gestion explicite d'`OPTIONS`. Redéployé (v2) — aucune modification frontend nécessaire, juste redéployer la fonction. À reconfirmer par l'utilisateur.

**Pas encore fait** : script de génération des comptes de démonstration (nécessite l'API Admin Supabase, prévu Phase 8).

## 4. Frontend — ce qui est fait

- Scaffold Vite + JS vanilla (pas de framework lourd), cible `es2017` + polyfills legacy (`@vitejs/plugin-legacy`) pour le navigateur intégré FiveM, build statique testé avec succès.
- Design system complet dans `src/styles/tokens.css` (couleurs, typographies, rayons, ombres — toute la charte y est centralisée).
- Logo recréé en SVG vectoriel propre (`src/assets/logo.svg`), fidèle au triangle "A" doré existant.
- Routeur hash minimal (`src/lib/router.js`) — robuste sur GitHub Pages sans configuration serveur.
- Client Supabase + conversion identifiant → e-mail synthétique (`src/lib/supabaseClient.js`).
- Écrans fonctionnels : connexion, inscription prospect, page d'accueil publique (contenu piloté par `site_content` avec repli si hors-ligne).
- Coquille commune (sidebar catégorisée repliable + topbar + notifications temps réel via Supabase Realtime) partagée par les interfaces internes — `src/lib/shell.js` + `src/lib/notifications.js` + `src/lib/features.js` (résolution générique `feature_registry`/`permission_grants`, réutilisée par Client et Employé).
- Captures d'écran validées visuellement (accueil public, connexion) — rendu conforme à l'identité demandée.
- **Interface Client (phase 3, 11 écrans)** : tableau de bord, comptes (détail + historique), virements (interne / bénéficiaire / IBAN externe avec résolution en direct via `resolve_account_by_iban`), bénéficiaires, lingots d'or (achat banque + marché de revente), coffres-forts, prêts professionnels (demande, échéancier, remboursement anticipé), consulting premium, documents, support (tickets en fil de discussion), paramètres. Ajoute aussi l'écran et la route manquante pour le rôle `prospect` (demande d'adhésion), qui comblait un trou de routing (boucle de redirection après inscription).
- **Interface Employé (phase 4, 16 écrans)** : tableau de bord (compteurs des files d'attente), recherche clients (fiche complète : comptes, catégories dynamiques, prêts), demandes d'adhésion (prise en charge + décision), ouverture de compte au guichet (crée maintenant directement le compte du client via l'Edge Function `create-account`, en plus de l'option de relier une demande à un profil déjà auto-inscrit), file d'attente, traitement des virements/lingots (banque + marché)/coffres-forts, réception des prêts (décision finale réservée à l'admin), consulting, rapport de caisse (lecture seule), alertes fraude (signalement manuel + traitement), support (vue personnel sur tous les tickets), journal d'activité, paramètres.
- **Interface Admin (phase 5, 23 écrans)** : reprend tous les écrans Employé (mêmes fonctionnalités, coquille et routes `/admin/*` dédiées) et ajoute : décision finale des prêts (`admin_decide_loan`, sur `/admin/loans`), registre complet des lingots + frappe de nouveaux lingots (`mint_gold_bar`/`admin_update_gold_bar`, sur `/admin/gold`), correction manuelle du rapport de caisse (`admin_adjust_cashier_report`, sur `/admin/cashier`), gestion des employés/rôles (`/admin/staff` — changement de rôle/titre/statut d'un profil existant via `profiles_admin_all`), permissions génériques (`/admin/permissions` — registre de fonctionnalités + exceptions par compte), comptes IRS (`/admin/irs-accounts` — octroi/révocation), masquage compte-ou-transaction par interface (`/admin/visibility`), pilotage économique complet + exceptions par client (`/admin/economic-settings`), CMS du site (`/admin/cms` — édition JSON des sections lues par la page d'accueil publique), configuration système (`/admin/system` — `maintenance_mode`/`announcement_banner` et autres réglages `category='system'`). Dans le détail client (`/admin/clients`), ajoute aussi le gel/suspension du profil, le gel/fermeture de compte et l'édition des seuils individuels (solde minimum, minimum de virement, note de confiance).
- `/admin/staff` propose maintenant **deux façons** de donner un accès personnel : créer directement un compte employé/admin/IRS (identifiant + mot de passe, via l'Edge Function `create-account`), ou promouvoir un profil déjà inscrit (prospect/client) en changeant son rôle. Les deux mènent au même résultat, la création directe est la voie recommandée pour un nouveau membre du personnel.
- **Interface IRS (phase 6, 6 écrans)** : tableau de bord (statistiques agrégées via `irs_stats`), clients, comptes, transactions, lingots d'or, paramètres (profil/mot de passe). Strictement lecture seule — le rôle IRS n'a **aucune** policy RLS sur les tables de base (deny by default) : toutes les lectures passent exclusivement par les fonctions `SECURITY DEFINER` dédiées `irs_stats`/`irs_list_clients`/`irs_list_accounts`/`irs_list_transactions`/`irs_list_gold_bars` (0003_policies.sql), qui appliquent elles-mêmes le masquage par interface (`visibility_masks` géré par l'admin) et ne contiennent aucune capacité d'écriture. La coquille IRS affiche un bandeau permanent rappelant l'accès lecture seule (nouvelle option `banner` ajoutée à `src/lib/shell.js`, réutilisable par les autres rôles si besoin). La trésorerie de la banque est explicitement exclue du registre des comptes IRS (`irs_list_accounts` filtre `is_bank_treasury = false`).
- Toutes les écritures qui touchent à l'argent passent exclusivement par les fonctions `SECURITY DEFINER` existantes (aucun insert/update direct de solde, conforme au principe central du cahier des charges) ; les tables non-financières de configuration/administration (file d'attente, alertes fraude, catégories clientèle, registre de fonctionnalités, permissions, paramètres économiques, comptes IRS, contenu du site) acceptent une écriture directe côté staff/admin, explicitement autorisée par les policies RLS `is_staff()`/`is_admin()`.
- Build Vite vérifié sans erreur après chaque phase + test en navigateur headless (Playwright) sans erreur JavaScript sur les routes publiques.

## 5bis. Lot de fonctionnalités additionnel (17/08/2026)

Suite à une demande explicite et détaillée de l'utilisateur listant 10 améliorations, traitées dans cette mise à jour :

- **Identifiant Discord** : nouvelle colonne `profiles.discord_id` (migration `0008_discord_and_audit`), renseignable à l'inscription prospect (`/signup`), à la création directe d'un compte (Edge Function `create-account`, désormais en v3 avec le paramètre `discordId`) et modifiable pour un profil existant depuis `/admin/staff`. Sert de base à la réinitialisation de mot de passe via Discord (point bloquant, voir §5).
- **Cours de l'or à l'once** : affiché sur `/client/gold` (conversion `prix/gramme × 31,1034768`) et sur `/admin/economic-settings` en regard du paramètre `gold_price_per_gram`.
- **Lingots en circulation / possédés** : `/admin/gold` et le tableau de bord admin affichent désormais le nombre de lingots en circulation (statut `sold`) et leur poids total ; `/client/gold` affiche le poids et la valeur totale possédée par le client ; `/admin/clients` affiche les lingots possédés par le client sélectionné (nouvelle fonction `getClientGoldBars`).
- **Catégories clientèle** : `/employee/clients` et `/admin/clients` permettent désormais de filtrer la recherche par catégorie (nouvelle fonction `getClientIdsInCategory`) et de créer une nouvelle catégorie directement depuis l'écran (nouvelle fonction `createClientCategory`, autorisée par la policy RLS `client_categories_staff_write` déjà en place).
- **Adaptation mobile** : règles CSS responsives ajoutées dans `src/styles/base.css` (`@media max-width: 720px`) — toutes les grilles à colonnes multiples repassent en une seule colonne, les tableaux larges deviennent défilables horizontalement au lieu d'écraser le texte, padding/typographie réduits, `font-size: 16px` sur les champs de saisie pour éviter le zoom automatique iOS.
- **Journal d'activité enrichi** : la migration `0008` ajoute des appels `log_audit(...)` à toutes les fonctions de décision métier qui n'en avaient pas encore (virements, lingots, coffres, prêts, adhésion, support, consulting), avec le nom du client/montant/contexte dans le détail. `/admin/audit` et `/employee/audit` affichent maintenant le rôle de l'auteur et une traduction lisible de l'action + du détail (nouvelles fonctions partagées `auditActionLabel`/`auditDetailsText` dans `src/lib/format.js`) au lieu du nom brut de la fonction SQL et d'un JSON illisible.
- **Suivi de virement côté client** : `/client/transfers` affiche désormais des statuts spécifiques au virement plutôt que les libellés génériques partagés — "Demande envoyée" (pending) → "En cours de traitement" (processing, dès qu'un employé/admin prend en charge) → "Virement effectué" (validated) → "Refusé" (rejected). Bug corrigé au passage : les virements **internes** (entre comptes du même client) ne notifiaient jamais le personnel (`submit_transfer` ne le faisait que pour les virements externes) — ils pouvaient rester invisibles indéfiniment ; corrigé dans la migration `0008`.
- **Non fait dans ce lot — bloqué sur des éléments externes que je ne peux pas créer moi-même** : réinitialisation de mot de passe par Discord (nécessite un bot Discord) et CAPTCHA anti-spam/DDoS (nécessite une clé Cloudflare Turnstile). Détail et marche à suivre en §5, points 6 et 7.

## 5bis-ter. CAPTCHA, réinitialisation Discord, liens Discord, messagerie, trésorerie (18/08/2026)

Quatre demandes traitées à la suite, dans cet ordre :

- **CAPTCHA Cloudflare Turnstile** : clé de site fournie par l'utilisateur (`0x4AAAAAAES6BOvwjwDOOqdz`), intégrée sur `/login` et `/signup` (`src/lib/turnstile.js`, chargement dynamique du script Turnstile, jeton transmis à `signInWithUsername`/`signUpProspect` puis à Supabase Auth via `options.captchaToken`). Ancien point bloquant §5.7 levé côté frontend — reste à l'utilisateur d'activer "Bot and Abuse Protection" avec la Secret Key côté tableau de bord Supabase (Authentication > Attack Protection), réglage que je ne peux pas activer moi-même.
- **Réinitialisation de mot de passe par Discord** : nouvelle table `password_reset_codes` (migration `0009`) + deux Edge Functions dédiées `verify_jwt: false` (`request-password-reset`, `confirm-password-reset`) + assistant en 2 étapes `/forgot-password`. Code à 6 chiffres, haché SHA-256, expire en 15 min, 5 tentatives max, cooldown 60s, message générique anti-énumération. Le bot Discord envoie le code par message privé via l'API REST Discord (`DISCORD_BOT_TOKEN`, configuré par l'utilisateur dans Supabase Edge Functions > Secrets — je n'ai jamais reçu ni demandé ce jeton). Ancien point bloquant §5.6 levé.
- **Liens Discord sur tout le site** : `https://discord.gg/ptJ9ksjpgs` ajouté dans la catégorie Support/Services des 4 interfaces internes (Client/Employé/Admin/IRS — IRS n'avait auparavant aucune catégorie Support), en haut à droite de la barre de navigation publique (à côté de "Se connecter"), et sous le formulaire de connexion ("Rejoindre le Discord").
- **Messagerie inter-rôles** (migration `0010`) : nouvelles tables `message_threads`/`thread_messages`, RLS strictement limitée aux deux participants (aucune policy d'écriture — tout passe par les fonctions `SECURITY DEFINER` `create_message_thread`/`send_thread_message`/`mark_thread_read`/`close_message_thread`/`list_my_message_threads`/`list_messageable_contacts`). N'importe quelle paire de rôles peut échanger (Client↔Employé, Client↔Admin, Client↔IRS, Employé↔Admin, Employé↔IRS, Admin↔IRS, etc.) **sauf Client↔Client**, refusé côté serveur avec un message explicite. Nouvel écran "Messagerie" partagé (`src/pages/shared/messagesScreen.js`) branché dans les 4 interfaces (`/client/messages`, `/employee/messages`, `/admin/messages`, `/irs/messages`), avec composition d'un nouveau message (choix du destinataire parmi les contacts autorisés), fil de discussion, clôture, notifications en temps réel (Realtime activé sur les deux nouvelles tables, et sur `notifications` qui n'y était pas encore malgré l'abonnement déjà en place côté frontend). Le bandeau "lecture seule" de l'interface IRS a été reformulé pour préciser que la messagerie fait exception.
- **Trésorerie — fonds propres vs actif en gestion** : `decide_membership_request()` et `finalize_manual_account_opening()` créditaient auparavant le compte du nouveau client sans jamais débiter la banque (l'argent apparaissait depuis rien). Les deux fonctions débitent désormais le compte de trésorerie (`bank_treasury_account_id()`) du même montant que le dépôt initial, et la transaction `cash_deposit` correspondante a maintenant un `from_account_id` renseigné. Nouvelle fonction `admin_treasury_stats()` (admin uniquement) exposant trois montants distincts, affichés en cartes sur le tableau de bord admin : **fonds propres** (solde réel du compte de trésorerie — l'argent qui appartient à la banque), **actif en gestion** (somme des soldes de tous les comptes clients actifs — l'argent des clients géré par la banque), **solde total** (somme des deux).
- **Correction des liens de notification cassés** : plusieurs fonctions notifiaient avec un chemin qui ne correspondait à aucune route réelle (segments `/operations/...` inexistants, `/admin/clients/membership`, `/admin/clients/openings`, liens `null` sur les notifications prospect). Corrigé dans `submit_transfer`, `decide_transfer`, `submit_gold_bank_purchase`, `submit_market_purchase`, `decide_gold_bank_purchase`, `decide_market_purchase`, `submit_safe_request`, `confirm_safe_rental`, `submit_loan_request`, `decide_membership_request`, `finalize_manual_account_opening`, `claim_membership_request` — un clic sur n'importe quelle notification amène désormais bien à l'écran attendu. Corrigé aussi côté frontend (`src/lib/shell.js`) : `notify_all_staff()` envoie le même lien `/employee/...` aux employés et aux admins (sauf notifications réservées à l'admin) ; un admin qui cliquait dessus se faisait rediriger vers son propre tableau de bord par la garde de rôle (`guardedRoleRender`) au lieu de l'écran visé — le lien est maintenant adapté au rôle réel avant la navigation.
- **Navigation de la page d'accueil publique** : les liens d'ancre "Services" et "Actualités" (`#services`, `#news`) étaient interceptés à tort par le routeur global basé sur le hash (qui traite tout changement de hash comme une tentative de route et renvoyait vers l'accueil, rechargement complet, au lieu de défiler jusqu'à la section). Corrigé avec un défilement en douceur géré en JavaScript (`scrollIntoView`), sans passer par le routeur.

## 5. Ce qui bloque une mise en ligne réelle

1. ~~Connexion Supabase~~ ✅ Fait : projet recréé (`newpad-bnw-v2`, réf. `vdbuvltfwulsxqjpwzhr`) suite à l'incident de suppression du 15/08, migrations redéployées, `.env.local`/`.env.production` (local et sur GitHub) mis à jour avec l'URL et la clé anon du nouveau projet.
2. ~~Désactiver "Confirm email"~~ ✅ Fait sur le nouveau projet (`newpad-bnw-v2`) — confirmé par l'utilisateur.
3. ~~Dépôt GitHub~~ ✅ Fait, avec une méthode différente de celle prévue : l'environnement cloud bloque tout push git/API sortant vers GitHub (proxy interne : "repository not in this session's authorized repository set", aucun outil disponible pour lever cette restriction). Contournement établi et répété depuis : le dossier `src/` mis à jour est livré sous forme de zip à l'utilisateur, qui le glisse-dépose à la racine du dépôt via "Add file → Upload files" sur `github.com/bouzoumacabre/newpad" (GitHub reconnaît les chemins existants et met à jour/ajoute les fichiers automatiquement).
4. ~~Déploiement GitHub Pages~~ ✅ **Fait et vérifié en ligne** : `https://bouzoumacabre.github.io/newpad/`. Workflow `.github/workflows/deploy.yml` (build Vite + `actions/deploy-pages`) opérationnel, se redéclenche à chaque push sur `main`. Un blocage initial ("Get Pages site failed — Not Found") a été résolu en re-sélectionnant la source "GitHub Actions" dans Settings > Pages (bascule sur "Deploy from a branch" puis retour, pour forcer l'activation côté GitHub) avant de relancer le job.
5. ~~Edge Function de création de comptes~~ ✅ **Fait** : `create-account` déployée sur le projet Supabase, utilisée par l'ouverture de compte au guichet (Employé/Admin) et par `/admin/staff` pour créer directement un employé/admin/IRS. Voir §3 pour le détail technique. Non testée en conditions réelles depuis ce sandbox (réseau sortant vers Supabase bloqué ici) — à valider par l'utilisateur en jeu.
6. ~~Réinitialisation de mot de passe via Discord~~ ✅ **Fait côté code** (voir §5bis-ter) : code à usage unique envoyé en message privé par le bot. Reste à la charge de l'utilisateur (je ne peux pas le faire moi-même) : créer le bot Discord, l'inviter sur le serveur Hurricane FA avec la permission d'envoyer des messages privés, et renseigner `DISCORD_BOT_TOKEN` dans Supabase Edge Functions > Secrets.
7. ~~CAPTCHA anti-spam/DDoS~~ ✅ **Fait côté code** (voir §5bis-ter) : widget Turnstile intégré sur `/login` et `/signup` avec la Site Key fournie. Reste à la charge de l'utilisateur : activer « Bot and Abuse Protection » avec la Secret Key dans Authentication > Attack Protection sur le tableau de bord Supabase (réglage que je ne peux pas activer moi-même).

**Important pour la suite** : je n'ai toujours pas d'accès en écriture (push) vers GitHub depuis cet environnement — chaque mise à jour future du frontend devra repasser par la même méthode manuelle (zip + upload web par l'utilisateur), sauf si un moyen d'autoriser le dépôt pour la session apparaît côté compte Claude/Cowork.

## 6. Prochaines étapes

1. ~~Connecter Supabase → créer le projet et exécuter les migrations.~~ ✅ Fait.
2. ~~Dépôt GitHub + déploiement GitHub Pages.~~ ✅ Fait — site en ligne à `https://bouzoumacabre.github.io/newpad/`.
3. ~~Construire l'interface Client écran par écran (phase 3).~~ ✅ Fait.
4. ~~Construire l'interface Employé (phase 4).~~ ✅ Fait.
5. ~~Construire l'interface Admin (phase 5).~~ ✅ Fait — 23 écrans (voir §4).
6. ~~Construire l'interface IRS (phase 6).~~ ✅ Fait — 6 écrans, lecture seule stricte via les fonctions `irs_*` (voir §4).
7. ~~Edge Function de création de comptes.~~ ✅ Fait — `create-account` déployée, intégrée au guichet et à `/admin/staff` (voir §3 et §5, point 5).
8. Notifications temps réel : le mécanisme (`src/lib/notifications.js`, Supabase Realtime) est déjà branché dans la coquille commune et fonctionne pour les 4 rôles internes (Client, Employé, Admin, IRS). Reste à vérifier ce mécanisme en conditions réelles en jeu (phase 7).
9. Données de démo réalistes, tests de bout en bout — y compris la nouvelle création de compte directe (Edge Function) — en conditions réelles (phase 8).
10. ~~Lot de fonctionnalités additionnel (Discord ID, cours de l'or à l'once, lingots en circulation/possédés, catégories clientèle filtrables, adaptation mobile, journal d'activité enrichi, suivi de virement)~~ ✅ Fait — voir §5bis.
11. ~~CAPTCHA Turnstile + réinitialisation de mot de passe Discord + liens Discord sur tout le site + messagerie inter-rôles + trésorerie fonds propres/actif en gestion + correction des liens de notification + correction navigation d'ancre de l'accueil~~ ✅ Fait côté code — voir §5bis-ter pour le détail. Reste à la charge de l'utilisateur : créer/inviter le bot Discord et renseigner `DISCORD_BOT_TOKEN`, activer la Secret Key Turnstile côté Supabase.
12. Prochaine étape immédiate : vérification en jeu de l'ensemble du lot livré aujourd'hui (envoyer un message d'un rôle à l'autre pour vérifier la messagerie, valider une nouvelle demande d'adhésion et vérifier au tableau de bord admin que fonds propres/actif en gestion évoluent comme attendu, cliquer sur plusieurs notifications pour vérifier les redirections, tester les liens "Services"/"Actualités" de l'accueil). Une fois le bot Discord et la Secret Key Turnstile en place côté utilisateur, confirmer que la réinitialisation de mot de passe et le CAPTCHA fonctionnent de bout en bout en jeu.
